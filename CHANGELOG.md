<div align="center">

# 📜 تاریخچه تغییرات CarTouch

</div>

<div dir="rtl">

تغییرات مهم هر نسخه، اصلاحات امنیتی و باگ‌های رفع‌شده، و فهرست مشکلات شناخته‌شده‌ی فعلی در این فایل ثبت می‌شود.
معرفی و راه‌اندازی در [`README.md`](./README.md) و مشخصات فنی در [`CarTouch_V2_SPEC.md`](./CarTouch_V2_SPEC.md) است.

## 📑 فهرست

- [v2.0 — Learn Mode و دیتابیس خودروی شخصی](#v20--learn-mode-و-دیتابیس-خودروی-شخصی)
- [v1.0 — اصلاحات امنیتی و رفع باگ](#v10--اصلاحات-امنیتی-و-رفع-باگ)
- [🚧 مشکلات شناخته‌شده و کارهای باقی‌مانده](#-مشکلات-شناختهشده-و-کارهای-باقیمانده)

---

## v2.0 — Learn Mode و دیتابیس خودروی شخصی

**سپتامبر ۲۰۲۶**

### ✨ افزوده شد

سه منبع فرمان اکنون کنار هم کار می‌کنند: فایل‌های **DBC** آماده، فرمان‌های **یادگرفته‌شده** از CAN Bus واقعی، و فرمان‌های **دستی** (ورود مستقیم CAN ID/بایت). هر فرمان یادگرفته/دستی تا تأیید صریح کاربر `UNVERIFIED` می‌ماند و قابل اجرا نیست.

| فایل | تغییر |
|---|---|
| `custom_vehicle.h` | 🆕 ساختارهای `LearnedCommand` و `CustomVehicleProfile` |
| `custom_vehicle_store.cpp/.h` | 🆕 ذخیره‌ی JSON پروفایل‌ها در SPIFFS |
| `learn_engine.cpp/.h` | 🆕 موتور یادگیری (baseline capture + diff + رتبه‌بندی کاندید) |
| `active_profile_manager.cpp/.h` | 🆕 یکپارچه‌ساز منابع فرمان (DBC / Learned / Manual) |
| `vehicle_control.cpp/.h` | ♻️ بازنویسی: دیگر CAN ID هاردکد ندارد و از `ActiveProfileManager` می‌خواند. API قدیم (`lockAllDoors` و…) حفظ شد |
| `config.h` | ➕ ثابت‌های `MAX_CUSTOM_VEHICLES`، `LEARN_BASELINE_MS` و… |
| `webserver.cpp/.h` | ➕ endpoint های REST مدیریت پروفایل + پیام‌های WebSocket `learn_*` / `verify_*` (همه پشت همان احراز هویت v1.0) |
| `tft_ui.cpp/.h` | ➕ تب چهارم «یادگیری» + مودال‌های Wizard / ورود دستی / تأیید |
| `main.cpp` | ➕ سیم‌کشی ماژول‌های جدید (بدون تغییر جریان اصلی setup/loop) |
| `index.html`, `app.js`, `style.css` | ➕ تب یادگیری و منطق WebSocket مربوط به آن |

**زیرساخت:** فایل `.github/workflows/CarTouch-build.yml` با هر push فریمویر و ایمیج SPIFFS را در GitHub Actions می‌سازد.

### 🛡️ قوانین ایمنی رعایت‌شده

جزئیات در بخش ۸ سند SPEC:

- Learn Mode هرگز `sendMessage` صدا نمی‌زند — فقط شنود.
- هیچ فرمان Learned/Manual بدون `status == VERIFIED` اجرا نمی‌شود (چک در دو لایه: `ActiveProfileManager::resolveCommand` و `VehicleControl::_execute`).
- همان محدودیت نرخ v1.0 (۱۵۰ms سطح پایین، ۳۰۰ms وب) روی فرمان‌های جدید هم اعمال می‌شود.
- Import پروفایل همیشه همه‌ی فرمان‌ها را به `UNVERIFIED` تنزل می‌دهد.

### 📝 مستندات

- `README.md` بازنویسی و با کد واقعی تطبیق داده شد؛ ادعای «۳۰۰+ مدل خودرو» و چک‌لیست تحویل پروژه حذف شد و بخش «وضعیت و محدودیت‌ها» و «اولین اجرا» اضافه شد.
- درایور نمایشگر در مستندات یکدست شد: **ILI9341**، مطابق `platformio.ini` و `config.h`. (در دورهای قبلی مستندات بین ILI9341 و ST7796 جابه‌جا شده بود.)
- `CODE_REVIEW.md` حذف و محتوای مفیدش (باگ‌های رفع‌شده + مشکلات باقی‌مانده) در همین فایل ادغام شد.
- شمار فایل‌های DBC در مستندات از ۵۸ به ۵۷ اصلاح شد (شمارش واقعی `data/dbc`).

---

## v1.0 — اصلاحات امنیتی و رفع باگ

### 🔐 امنیت

**۱. احراز هویت WebSocket** *(مهم‌ترین مشکل)*
قبلاً هرکسی که IP دستگاه را می‌دانست، بدون رد شدن از صفحه‌ی لاگین می‌توانست به `/ws` وصل شود و `lock`, `unlock`, `trunk`, `alarm` و… بفرستد. اکنون:
- بعد از لاگین موفق، یک توکن تصادفی ۱۶ بایتی ساخته می‌شود.
- فرانت‌اند آن را از `/api/session-token` (که خودش Basic Auth دارد) می‌گیرد و به‌عنوان اولین پیام می‌فرستد: `{"type":"auth","token":"..."}`
- تا این پیام تأیید نشود، هیچ `command`ای پذیرفته نمی‌شود.
- توکن بعد از ۱۵ دقیقه بی‌فعالیتی منقضی می‌شود (`SESSION_TOKEN_TIMEOUT`).

> ⚠️ این یک لایه‌ی دفاعی سبک و مناسب دستگاه embedded است، نه جایگزین HTTPS. بدون HTTPS خود توکن روی شبکه رمزنگاری نمی‌شود.

**۲. رمزهای پیش‌فرض و صفحه‌ی تغییر رمز**
- پرچم `forcePasswordChange` در اولین بوت `true` است؛ `isUsingDefaultPassword()` آن را چک می‌کند و `main.cpp` هم در Serial و هم روی TFT هشدار نشان می‌دهد.
- `setWebPassword()` فقط رمزی را می‌پذیرد که حداقل ۸ کاراکتر و متفاوت از رمز پیش‌فرض (`"cartouch"`) باشد.
- **وب:** فرم تغییر رمز در تب تنظیمات (`/api/change-password`)؛ بعد از تغییر، session قبلی باطل می‌شود و اگر رمز هنوز پیش‌فرض باشد، هشدار قرمز نمایش داده می‌شود.
- **TFT:** دکمه‌ی «🔐 تغییر رمز ورود» در تب تنظیمات، با صفحه‌ی دو فیلدی و کیبورد مجازی LVGL (کیبورد فقط با فوکوس فیلد نمایش داده می‌شود). تغییر نام کاربری فقط از وب ممکن است.

**۳. دور زدن احراز هویت در فایل‌های استاتیک**
`serveStatic("/", SPIFFS, "/")` هر فایل SPIFFS (از جمله `index.html`) را بدون `_authenticate()` سرو می‌کرد. با روت‌های صریح فقط برای `app.js` و `style.css` (غیرحساس) جایگزین شد؛ بقیه پشت احراز هویت ماند.

**۴. رمز خام روی Serial**
پرینت رمز وب در هر بوت حذف شد (فقط username چاپ می‌شود).

**۵. Rate Limiting** — دو لایه:
- `webserver.cpp`: هر کلاینت WebSocket حداکثر هر ۳۰۰ms یک فرمان.
- `vehicle_control.cpp`: صرف‌نظر از منبع فرمان، حداکثر هر ۱۵۰ms یک فرمان فیزیکی روی CAN ارسال می‌شود (جلوگیری از فشار مکانیکی به موتور شیشه/قفل).

### 🐛 باگ‌های رفع‌شده

| مشکل | فایل | رفع |
|---|---|---|
| **سرریز بافر CAN:** `data_length_code` بدون بررسی در حلقه‌ی کپی استفاده می‌شد و مقدار بزرگ‌تر از ۸ حافظه‌ی بیرون از `msg.data[8]` را خراب می‌کرد | `can_manager.cpp` | مقدار قبل از کپی به ۸ محدود می‌شود |
| **مصرف RAM (بحرانی):** `MAX_DBC_SIGNALS=200` آرایه‌ی `VehicleDB` را حدود **۱٫۲۲ مگابایت** می‌کرد (اندازه‌گیری با g++) و روی ESP32-S3 با ~۵۱۲KB SRAM بوت/لینک را خراب می‌کرد | `vehicle_db.h` | کاهش به ۲۴ |
| **پارسر DBC (بحرانی):** فرمت `sscanf` خط `BO_` یک فاصله‌ی اضافه قبل از `:` داشت؛ چون هیچ‌کدام از فایل‌های DBC چنین فاصله‌ای ندارند، **هیچ پیامی هرگز پارس نمی‌شد** | `vehicle_db.cpp` | فرمت اصلاح و با `sscanf` واقعی روی همه‌ی فایل‌های DBC آزمایش شد |
| **سرریز CAN ID:** چند فایل واقعی (GM، Ford) از ID گسترده‌ی بالاتر از `INT_MAX` استفاده می‌کنند و `int` + `%d` آن‌ها را منفی می‌کرد | `vehicle_db.cpp` | تغییر به `uint32_t` + `%u` |
| **اندیس منفی:** `_parseSignalLine()` فرض می‌کرد قبلش همیشه یک `BO_` معتبر پارس شده | `vehicle_db.cpp` | بررسی مستقل اضافه شد |
| **خط طولانی DBC:** خط بلندتر از ۱۲۷ کاراکتر بی‌صدا بریده و اشتباه پارس می‌شد | `vehicle_db.cpp` | هشدار در Serial |
| **بریده شدن فایل بزرگ:** رسیدن به سقف `MAX_DBC_MESSAGES` کاملاً بی‌صدا بود | `vehicle_db.cpp` | هشدار truncation |
| **فایل‌های ناموجود:** لیست خودروها به `toyota_camry.dbc`، `honda_civic.dbc`، `bmw_3series.dbc`، `generic_obd2.dbc` اشاره می‌کرد که در `data/dbc` وجود نداشتند و بارگذاری بی‌صدا شکست می‌خورد | `vehicle_db.cpp` | اشاره به فایل‌های واقعی؛ «Generic / OBD-II» بدون DBC تعریف شد |

---

## 🚧 مشکلات شناخته‌شده و کارهای باقی‌مانده

وضعیت هر مورد با کد فعلی تطبیق داده شده است.

### 🔴 مهم برای استفاده‌ی واقعی

| # | موضوع | جزئیات |
|:-:|---|---|
| 1 | **فرمان از DBC ممکن نیست** | برای خودروهای انتخاب‌شده از لیست DBC، `resolveCommand` عمداً پیام خطا برمی‌گرداند (اکثر DBC های OpenDBC فقط سیگنال «خواندن» دارند). فرمان فقط از Learn Mode یا ورود دستی می‌آید |
| 2 | **Listen-Only پیش‌فرض خاموش است** | طبق طراحی SPEC دستگاه باید همیشه اول در Listen-Only شروع کند؛ ولی `listenOnlyMode` در `config.cpp` پیش‌فرض `false` است |
| 3 | **Listen-Only در زمان اجرا فقط نرم‌افزاری است** | درایور TWAI فقط در `begin()` با حالت ذخیره‌شده نصب می‌شود؛ تغییر حالت در حین کار فقط فیلتر نرم‌افزاری را عوض می‌کند. تضمین سخت‌افزاری برای Learn Mode نیاز به متد «reconfigure سریع» در `CANManager` دارد (SPEC بخش ۳.۳) |
| 4 | **بدون HTTPS** | ترافیک وب و توکن نشست رمزنگاری نمی‌شود. پیشنهاد: HTTPS با گواهی self-signed |
| 5 | **کالیبراسیون تاچ نمونه است** | `touchCalibX/Y` در `tft_ui.cpp` مقادیر placeholder دارد و باید روی نمایشگر شما تنظیم شود |

### 🟡 محدودیت‌های فنی

| # | موضوع | جزئیات |
|:-:|---|---|
| 6 | **Motorola (`@0`) پشتیبانی نمی‌شود** | پارسر هر دو اندیان را یکسان (Intel) پردازش می‌کند؛ برای سیگنال‌های Motorola مقدار غلط می‌دهد |
| 7 | **سقف ۵۰ پیام در هر فایل DBC** | `MAX_DBC_MESSAGES=50`؛ فایل‌هایی مثل `toyota_2017_ref_pt.dbc` (۱۴۳ پیام) و `bmw_e9x_e8x.dbc` (۳۲۶ پیام) بریده می‌شوند. افزایش سقف، RAM بیشتری می‌خواهد |
| 8 | **فقط ۳ مدل DBC در لیست** | از ۵۷ فایل DBC موجود فقط سه فایل در `vehicle_db.cpp` وصل شده است |
| 9 | **`OBD2Reader::readAllPIDs` مسدودکننده است** | با `delay()` و مهلت‌های ۲۰۰–۵۰۰ms؛ اگر ECU جواب ندهد `loop()` را کند می‌کند و UI/WebSocket را می‌گیرد. نیاز به بازطراحی با state machine |
| 10 | **بدون Watchdog صریح** | فقط تأخیر کوتاه در `main.cpp` هست؛ watchdog اختصاصی ESP32 تنظیم نشده |
| 11 | **تغییر رمز از TFT، session وب را باطل نمی‌کند** | `setWebPassword` از TFT صدا زده می‌شود و `_sessionToken` پاک نمی‌شود؛ session باز تا انقضا معتبر می‌ماند. نیاز به callback بین `tft_ui` و `webserver` |
| 12 | **حجم SPIFFS** | `data/` حدود ۳٫۴ مگابایت است. پارتیشن SPIFFS در `default_16MB.csv` (طبق نسخه‌ی استاندارد Arduino-ESP32) حدود همین اندازه است، نه کل ۱۶MB. اگر `uploadfs` خطای حجم داد، DBC های غیرضروری را حذف کنید یا پارتیشن سفارشی بگذارید *(این مورد را با `buildfs` خودتان تأیید کنید)* |

### 💡 پیشنهادهای توسعه

- شبیه‌ساز CAN روی PC برای تست state machine بدون خودرو
- تست واحد منطق diff/candidate-ranking با داده‌ی شبیه‌سازی‌شده (با g++ خالص)
- وصل کردن سایر فایل‌های DBC به لیست انتخاب

### ✅ رفع‌شده

- `vehicle_control.cpp` دیگر CAN ID حدسی هاردکد را مسیر اصلی نمی‌کند؛ در v2.0 از `ActiveProfileManager` می‌خواند. (مسیر قدیمی `setCustomCANIDs()` فقط برای سازگاری عقب‌رو مانده و فعلاً هیچ کدی آن را صدا نمی‌زند.)
- پوشه‌ی `generator/` (اسکریپت‌های پایتون OpenDBC، حدود ۱٫۱ مگابایت) دیگر در `data/dbc` نیست؛ حجم DBC ها از ~۴٫۵MB به ~۳٫۵MB رسیده است.

</div>
