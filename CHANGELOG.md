# راهنمای اعمال اصلاحات پروژه CarTouch

## 🆕 نسخه v2.0 — Learn Mode / دیتابیس خودروی سفارشی

برای مشخصات کامل معماری این نسخه، به
[`CarTouch_V2_SPEC.md`](./CarTouch_V2_SPEC.md) مراجعه کنید. خلاصه‌ی
فایل‌های جدید/تغییریافته:

| فایل | نوع تغییر |
|---|---|
| `custom_vehicle.h` | 🆕 جدید: ساختارهای LearnedCommand، CustomVehicleProfile |
| `custom_vehicle_store.cpp/.h` | 🆕 جدید: ذخیره‌سازی JSON پروفایل‌های سفارشی در SPIFFS |
| `learn_engine.cpp/.h` | 🆕 جدید: موتور یادگیری (baseline capture + diff + رتبه‌بندی کاندید) |
| `active_profile_manager.cpp/.h` | 🆕 جدید: یکپارچه‌ساز منابع فرمان (DBC / Learned / Manual) |
| `vehicle_control.cpp/.h` | بازنویسی: دیگر CAN ID هاردکد ندارد؛ از ActiveProfileManager می‌خواند. API قدیم (lockAllDoors و...) کاملاً حفظ شده |
| `config.h` | افزوده: ثابت‌های MAX_CUSTOM_VEHICLES، LEARN_BASELINE_MS و... |
| `webserver.cpp/.h` | افزوده: endpoint های REST مدیریت پروفایل + پیام‌های WebSocket learn_*/verify_* (همه پشت همان auth v1.0) |
| `tft_ui.cpp/.h` | افزوده: تب چهارم «یادگیری» + مودال‌های Wizard/ورود دستی/تأیید |
| `main.cpp` | افزوده: سیم‌کشی ماژول‌های جدید (بدون تغییر جریان اصلی setup/loop) |
| `index.html`, `app.js`, `style.css` | افزوده: تب یادگیری، مودال‌های مربوطه، منطق کامل WebSocket برای Learn Mode |

### خلاصه‌ی قابلیت جدید

سه منبع فرمان اکنون کنار هم کار می‌کنند: فایل‌های **DBC** آماده (مثل
قبل)، فرمان‌های **یادگرفته‌شده** از CAN Bus واقعی با دکمه فیزیکی
خودرو، و فرمان‌های **دستی** که کاربر مستقیم CAN ID/بایت وارد می‌کند.
هر فرمان از این دو منبع اخیر تا تأیید صریح کاربر (که CAN ID/بایت
واقعی را نشان می‌دهد و نتیجه‌ی واقعی روی خودرو را می‌پرسد)
`UNVERIFIED` می‌ماند و قابل اجرا نیست.

### قوانین ایمنی رعایت‌شده (جزئیات کامل در بخش ۸ سند)

- Learn Mode هرگز `sendMessage` صدا نمی‌زند - فقط شنود
- هیچ فرمان Learned/Manual بدون `status == VERIFIED` قابل اجرا نیست
  (چک در دو لایه: `ActiveProfileManager::resolveCommand` و
  `VehicleControl::_execute`)
- همان rate-limit های v1.0 (۱۵۰ms سطح پایین، ۳۰۰ms وب) روی فرمان‌های
  جدید هم اعمال می‌شود
- Import پروفایل همیشه همه‌ی فرمان‌ها را به UNVERIFIED تنزل می‌دهد

---

## نحوه استفاده (v1.0 - تاریخچه)

این ۱۰ فایل، نسخه‌ی اصلاح‌شده‌ی فایل‌های هم‌نام در پروژه شما هستند.
کافیست هرکدام را دانلود کنید و در ریپوی گیت‌هاب خودتان **جایگزین** فایل قبلی
با همان اسم کنید (یا مستقیم از GitHub وب یا از طریق `git`).

فایل‌هایی که تغییر کرده‌اند:

| فایل | نوع تغییر |
|---|---|
| `webserver.h` | جدید: ساختار احراز هویت WebSocket |
| `webserver.cpp` | session token برای WebSocket + rate limit + endpoint تغییر رمز |
| `app.js` | سیستم توکن جدید + فرم تغییر رمز در صفحه وب |
| `index.html` | افزودن فرم تغییر رمز و برچسب هشدار به تب تنظیمات |
| `config.h` | حذف تکیه بر رمز ثابت، افزودن پرچم اجبار تغییر رمز |
| `config.cpp` | افزودن توابع `isUsingDefaultPassword()` و `setWebPassword()` |
| `can_manager.cpp` | رفع باگ سرریز بافر (buffer overflow) در دریافت پیام CAN |
| `vehicle_control.h` | افزودن فیلدهای rate-limit |
| `vehicle_control.cpp` | افزودن rate-limit روی فرمان‌های فیزیکی (قفل/شیشه/...) |
| `vehicle_db.cpp` | رفع باگ اندیس منفی در پارسر DBC + هشدار خط طولانی |
| `main.cpp` | نمایش هشدار اگر رمز هنوز پیش‌فرض است |
| `tft_ui.h` | افزودن صفحه/فیلدهای تغییر رمز با کیبورد مجازی LVGL |
| `tft_ui.cpp` | پیاده‌سازی کامل صفحه تغییر رمز روی نمایشگر لمسی |

## خلاصه هر اصلاح (چرا و چطور)

### ۱. امنیت WebSocket (مهم‌ترین مشکل)
**قبلاً:** هرکسی که IP دستگاه (یا SSID شبکه AP) را می‌دانست، بدون رد شدن
از صفحه لاگین می‌توانست مستقیم به `/ws` وصل شود و فرمان `lock`, `unlock`,
`trunk`, `alarm` و... بفرستد.

**الان:**
- بعد از لاگین موفق در `/login`، یک توکن تصادفی ۱۶ بایتی (session token)
  ساخته می‌شود.
- فرانت‌اند (`app.js`) این توکن را از `/api/session-token` می‌گیرد (که خودش
  Basic Auth دارد) و به‌عنوان اولین پیام روی WebSocket می‌فرستد:
  `{"type":"auth","token":"..."}`
- تا این پیام تایید نشود، سرور هیچ `command`ای را قبول نمی‌کند.
- توکن بعد از ۱۵ دقیقه بی‌فعالیتی منقضی می‌شود (`SESSION_TOKEN_TIMEOUT`).

⚠️ **توجه:** این یک لایه دفاعی سبک مناسب دستگاه embedded است، نه یک
سیستم کامل مثل JWT+HTTPS. اگر امکانش هست، در آینده HTTPS (با گواهی
self-signed) هم روی ESPAsyncWebServer فعال کنید تا خود توکن هنگام
انتقال روی شبکه رمزنگاری‌شده باشد (الان شبکه AP باز/WPA2 است ولی
HTTP ساده رمزنگاری ندارد).

### ۲. رمزهای پیش‌فرض + صفحه تغییر رمز (کامل شد ✅)
- پرچم `forcePasswordChange` در تنظیمات اضافه شد که در اولین بوت `true` است.
- تابع `isUsingDefaultPassword()` این را چک می‌کند و `main.cpp` هنگام
  بوت هم در Serial Monitor و هم روی صفحه TFT هشدار نشان می‌دهد.
- تابع `setWebPassword()` رمز جدید را با این شرایط قبول می‌کند:
  - حداقل ۸ کاراکتر
  - متفاوت از رمز پیش‌فرض `"cartouch"`

**در صفحه وب:** تب تنظیمات یک فرم تغییر رمز دارد (`index.html` + `app.js`).
بعد از ورود موفق داده‌ها به `/api/change-password` (که خودش Basic Auth
لازم دارد) فرستاده می‌شوند. بعد از تغییر موفق، session قبلی باطل می‌شود
و صفحه خودش رفرش می‌شود تا کاربر با رمز جدید دوباره وارد شود. یک برچسب
هشدار قرمز هم بالای فرم نمایش داده می‌شود اگر هنوز رمز پیش‌فرض فعال باشد.

**در صفحه لمسی TFT:** در تب تنظیمات یک دکمه قرمز «🔐 تغییر رمز ورود»
اضافه شد. با فشردن آن یک صفحه مودال با دو فیلد (رمز جدید / تکرار رمز)
و کیبورد مجازی LVGL باز می‌شود. کیبورد فقط وقتی یک فیلد فوکوس بگیرد
نمایش داده می‌شود (برای صرفه‌جویی در فضای صفحه کوچک ۲.۴ اینچی). اعتبارسنجی
(حداقل ۸ کاراکتر، تطابق دو فیلد) هم سمت TFT هم دوباره سمت `setWebPassword()`
انجام می‌شود. توجه: تغییر نام کاربری فقط از وب امکان‌پذیر است؛ از TFT فقط
رمز تغییر می‌کند (برای سادگی کیبورد).

⚠️ نکته امنیتی: بعد از تغییر رمز از TFT، اگر یک session وب باز باشد،
همچنان معتبر می‌ماند تا منقضی شود (چون `setWebPassword` از TFT صدا زده
می‌شود، نه از webserver.cpp، پس `_sessionToken` پاک نمی‌شود). اگر می‌خواهید
این هم لغو شود، باید یک callback بین tft_ui و webserver برقرار کنید —
اگر لازم دارید بگویید تا اضافه کنم.

### ۳. باگ Buffer Overflow در CAN
در `can_manager.cpp`، `receiveMessage()` مقدار `data_length_code` را
بدون بررسی مستقیم در حلقه‌ی کپی داده استفاده می‌کرد. اگر این مقدار
(به هر دلیلی — باگ درایور، نویز باس) بزرگ‌تر از ۸ باشد، حافظه خارج از
`msg.data[8]` بازنویسی می‌شود. الان قبل از کپی، به ۸ محدود (clamp) می‌شود.

### ۴. Rate Limiting
دو لایه اضافه شد:
- در `webserver.cpp`: هر کلاینت WebSocket حداکثر هر ۳۰۰ میلی‌ثانیه یک
  فرمان می‌تواند بفرستد.
- در `vehicle_control.cpp`: صرف‌نظر از منبع فرمان (وب، TFT، هرچیز دیگر
  در آینده)، حداکثر هر ۱۵۰ میلی‌ثانیه یک فرمان فیزیکی واقعاً روی CAN
  Bus ارسال می‌شود. این از آسیب مکانیکی به موتور شیشه/قفل توسط
  اسپم فرمان جلوگیری می‌کند.

### ۵. باگ اندیس منفی در پارسر DBC
در `vehicle_db.cpp`، `_parseSignalLine()` فرض می‌کرد همیشه قبلش یک
خط `BO_` معتبر پارس شده. حالا خودش هم این را مستقل چک می‌کند.

### ۶. هشدار خط طولانی DBC
اگر یک خط در فایل DBC از ۱۲۷ کاراکتر بلندتر باشد، حالا هشدار در
Serial چاپ می‌شود (قبلاً بی‌صدا truncate و پارس اشتباه می‌شد).

## کارهایی که هنوز باقی مانده (پیشنهادی، نه انجام‌شده)

1. **HTTPS** برای رمزنگاری کامل ترافیک بین مرورگر و دستگاه
2. **پارسر DBC قوی‌تر** — پارسر فعلی مبتنی بر `sscanf` است و روی فایل‌های
   واقعی OpenDBC (که فرمت‌بندی متنوع‌تری دارند) ممکن است شکست بخورد.
   اگر لازم دارید می‌توانم این را هم بازنویسی کنم.
3. **Watchdog صریح ESP32** برای جلوگیری از فریز در صورت کند بودن پاسخ CAN
4. **لغو همزمان session وب هنگام تغییر رمز از TFT** (توضیح داده شد بالا)

اگر می‌خواهید هرکدام از این موارد باقیمانده را هم پیاده کنم، بگویید کدام
اولویت دارد.
