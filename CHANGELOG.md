<div align="center">

# 📜 تاریخچه تغییرات CarTouch

</div>

<div dir="rtl">

تغییرات مهم هر نسخه، اصلاحات امنیتی و باگ‌های رفع‌شده، و فهرست مشکلات شناخته‌شده‌ی فعلی در این فایل ثبت می‌شود.
معرفی و راه‌اندازی در [`README.md`](./README.md) و مشخصات فنی در [`CarTouch_SPEC.md`](./CarTouch_SPEC.md) است.

## 📑 فهرست

- [v2.1 — اصلاحات نسخه‌ی تجاری (Watchdog، OBD2 غیرمسدودکننده، Endianness، کالیبراسیون واقعی)](#v21--اصلاحات-نسخهی-تجاری)
- [v2.0 — Learn Mode و دیتابیس خودروی شخصی](#v20--learn-mode-و-دیتابیس-خودروی-شخصی)
- [v1.0 — اصلاحات امنیتی و رفع باگ](#v10--اصلاحات-امنیتی-و-رفع-باگ)
- [🚧 مشکلات شناخته‌شده و کارهای باقی‌مانده](#-مشکلات-شناختهشده-و-کارهای-باقیمانده)

---

## v2.1 — اصلاحات نسخه‌ی تجاری

**سپتامبر ۲۰۲۶**

هدف این نسخه، رفع فهرست موارد «بحرانی برای استفاده‌ی واقعی» و
«محدودیت‌های فنی» بود که در انتهای این فایل (نسخه‌ی قبلی) فهرست شده
بودند. وضعیت گام‌به‌گام و کامل هر مورد در
[`PROGRESS_CHECKLIST.md`](./PROGRESS_CHECKLIST.md) نگه داشته می‌شود؛
اینجا فقط خلاصه‌ی نهایی می‌آید.

> ⚠️ این تغییرات به‌صورت دستی/منطقی بررسی شده‌اند (و مورد Endianness
> با یک شبیه‌سازی مستقل C++ تست شد)، ولی هنوز با toolchain واقعی
> PlatformIO کامپایل و روی خودروی واقعی تست نشده‌اند.

### ✨ رفع شد

| مشکل قدیمی | فایل‌ها | رفع |
|---|---|---|
| Watchdog نداشت (بند ۱۰ نسخه‌ی قبلی این فایل) | `main.cpp` | `esp_task_wdt` با timeout ۸ ثانیه اضافه شد؛ `esp_task_wdt_reset()` در ابتدای هر `loop()`. با شرط‌گذاری روی نسخه‌ی Arduino-ESP32 core (۲.x/۳.x) برای سازگاری با هر دو زنجیره‌ی ابزار |
| `OBD2Reader::readAllPIDs` مسدودکننده بود (بند ۹) | `obd2_reader.h`, `obd2_reader.cpp`, `main.cpp` | بازنویسی کامل به state machine (`update()`/`getLatestData()`)؛ صفر `delay()` در مسیر اصلی loop. توابع تک‌PID قدیمی به‌عنوان API سازگار با برچسب صریح `[BLOCKING]` نگه داشته شدند |
| Motorola (`@0`) پشتیبانی نمی‌شد (بند ۶) | `vehicle_db.h`, `vehicle_db.cpp` | نگاشت بیت جداگانه برای Motorola/Intel اضافه شد (`isBigEndian` در `DbcSignal` اکنون واقعاً استفاده می‌شود، نه فقط خوانده و دور ریخته). با یک تست مستقل خارج از پروژه روی سیگنال‌های واقعی `bmw_e9x_e8x.dbc` و `vw_mqb.dbc` تأیید شد |
| فقط ۳ مدل DBC وصل بود (بند ۸) | `vehicle_db.h`, `vehicle_db.cpp` | افزایش به ۳۸ مدل. فهرست کامل مدل‌های عمداً وصل‌نشده (فایل‌های رادار/ADAS جانبی، یا فایل‌های بسیار حجیم) با دلیل، در کامنت‌های `VehicleDB::begin()` مستند شد |
| سقف ۵۰ پیام در هر فایل DBC (بند ۷) | `vehicle_db.h` | افزایش به ۱۵۰ |
| کالیبراسیون تاچ نمونه/placeholder بود (بند ۵) | `tft_ui.h`, `tft_ui.cpp`, `config.h`, `config.cpp` | کالیبراسیون واقعی ۵نقطه‌ای با `TFT_eSPI::calibrateTouch()` در اولین بوت؛ نتیجه در حافظه‌ی غیرفرار ذخیره می‌شود؛ دکمه‌ی کالیبراسیون مجدد در تب تنظیمات |
| تغییر رمز از TFT، session وب را باطل نمی‌کرد (بند ۱۱) | `config.h`, `config.cpp`, `webserver.h`, `webserver.cpp` | callback سراسری (`registerPasswordChangeCallback`) اضافه شد؛ تغییر رمز از هر مسیر (TFT یا وب)، هم HTTP session و هم auth تمام کلاینت‌های WebSocket را باطل می‌کند |
| `listenOnlyMode` پیش‌فرض `false` بود (بند ۲) | `config.cpp`, `config.h` | پیش‌فرض به `true` اصلاح شد؛ دستگاه همیشه اول در Listen-Only بوت می‌شود |

### 🐛 دو باگ کشف‌شده و رفع‌شده در همین دور توسعه

این دو مورد حین پیاده‌سازی جدول بالا پیدا شدند (نه در فهرست قبلی این
فایل بودند):

- **باگ لینکر DRAM overflow:** تلاش اولیه برای انتقال `vehicleDB`
  (که بعد از افزایش سقف پیام DBC به حدود ۴۶۰ کیلوبایت رسیده بود) به
  PSRAM با `EXT_RAM_BSS_ATTR` روی آبجکت global انجام شده بود. این
  attribute بدون فعال بودن `CONFIG_SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY`
  در sdkconfig (که در کتابخانه‌های از پیش کامپایل‌شده‌ی Arduino-ESP32
  فعال نیست) بی‌صدا نادیده گرفته می‌شود و کل حجم در DRAM داخلی
  می‌ماند → خطای لینک. **رفع نهایی:** `vehicleDB` و وابسته‌هایش
  (`activeProfileManager`, `vehicleControl`) به پوینتر تبدیل و با
  `new` در `setup()` ساخته می‌شوند؛ تخصیص‌های بزرگ‌تر از ۴ کیلوبایت
  روی هسته‌ی PSRAM-فعال به‌طور خودکار از PSRAM سرو می‌شوند.
- **باگ کامپایل TFT_UI:** اشاره‌گر استاتیک `pThisUI` که تمام
  callback های لمسی (دکمه‌ها، صفحه‌ی رمز، Learn Wizard) به آن
  وابسته‌اند، در تبدیل پروژه از v1.0 به v2.0 به‌اشتباه حذف شده بود.
  دوباره اضافه شد.

### 🔍 اصلاح مستندات

- ادعای «OTA نداریم» که در نسخه‌های قبلی این فایل/README ثبت شده
  بود، غلط بود — بررسی کد نشان داد OTA (مسیر `/update`، آپلود
  firmware.bin/spiffs.bin) از قبل به‌طور کامل پیاده‌سازی شده است.
- بخش «مشکلات شناخته‌شده» در انتهای این فایل به‌طور کامل بازبینی و
  موارد رفع‌شده به بالا منتقل شدند.

---

## v2.0 — Learn Mode و دیتابیس خودروی شخصی

**سپتامبر ۲۰۲۶**

### ✨ افزوده شد

سه منبع فرمان اکنون کنار هم کار می‌کنند: فایل‌های **DBC** آماده، فرمان‌های **یادگرفته‌شده** از CAN Bus واقعی، و فرمان‌های **دستی** (ورود مستقیم CAN ID/بایت). هر فرمان یادگرفته/دستی تا تأیید صریح کاربر `UNVERIFIED` می‌ماند و قابل اجرا نیست.

| فایل | تغییر |
|---|---|
| `custom_vehicle.h` | 🆕 ساختارهای `LearnedCommand` و `CustomVehicleProfile` |
| `custom_vehicle_store.cpp/.h` | 🆕 ذخیره‌ی JSON پروفایل‌ها در SPIFFS |
| `learn_engine.cpp/.h` | 🆕 موتور یادگیری (baseline capture + diff + رتبه‌بندی کاندید) |
| `active_profile_manager.cpp/.h` | 🆕 یکپارچه‌ساز منابع فرمان (DBC / Learned / Manual) |
| `vehicle_control.cpp/.h` | ♻️ بازنویسی: دیگر CAN ID هاردکد ندارد و از `ActiveProfileManager` می‌خواند. API قدیم (`lockAllDoors` و…) حفظ شد |
| `config.h` | ➕ ثابت‌های `MAX_CUSTOM_VEHICLES`، `LEARN_BASELINE_MS` و… |
| `webserver.cpp/.h` | ➕ endpoint های REST مدیریت پروفایل + پیام‌های WebSocket `learn_*` / `verify_*` (همه پشت همان احراز هویت v1.0) |
| `tft_ui.cpp/.h` | ➕ تب چهارم «یادگیری» + مودال‌های Wizard / ورود دستی / تأیید |
| `main.cpp` | ➕ سیم‌کشی ماژول‌های جدید (بدون تغییر جریان اصلی setup/loop) |
| `index.html`, `app.js`, `style.css` | ➕ تب یادگیری و منطق WebSocket مربوط به آن |

**زیرساخت:** فایل `.github/workflows/CarTouch-build.yml` با هر push فریمویر و ایمیج SPIFFS را در GitHub Actions می‌سازد.

### 🛡️ قوانین ایمنی رعایت‌شده

جزئیات در بخش ۸ سند SPEC:

- Learn Mode هرگز `sendMessage` صدا نمی‌زند — فقط شنود.
- هیچ فرمان Learned/Manual بدون `status == VERIFIED` اجرا نمی‌شود (چک در دو لایه: `ActiveProfileManager::resolveCommand` و `VehicleControl::_execute`).
- همان محدودیت نرخ v1.0 (۱۵۰ms سطح پایین، ۳۰۰ms وب) روی فرمان‌های جدید هم اعمال می‌شود.
- Import پروفایل همیشه همه‌ی فرمان‌ها را به `UNVERIFIED` تنزل می‌دهد.

### 📝 مستندات

- `README.md` بازنویسی و با کد واقعی تطبیق داده شد؛ ادعای «۳۰۰+ مدل خودرو» و چک‌لیست تحویل پروژه حذف شد و بخش «وضعیت و محدودیت‌ها» و «اولین اجرا» اضافه شد.
- درایور نمایشگر در مستندات یکدست شد: **ILI9341**، مطابق `platformio.ini` و `config.h`. (در دورهای قبلی مستندات بین ILI9341 و ST7796 جابه‌جا شده بود.)
- `CODE_REVIEW.md` حذف و محتوای مفیدش (باگ‌های رفع‌شده + مشکلات باقی‌مانده) در همین فایل ادغام شد.
- شمار فایل‌های DBC در مستندات از ۵۸ به ۵۷ اصلاح شد (شمارش واقعی `data/dbc`).

---

## v1.0 — اصلاحات امنیتی و رفع باگ

### 🔐 امنیت

**۱. احراز هویت WebSocket** *(مهم‌ترین مشکل)*
قبلاً هرکسی که IP دستگاه را می‌دانست، بدون رد شدن از صفحه‌ی لاگین می‌توانست به `/ws` وصل شود و `lock`, `unlock`, `trunk`, `alarm` و… بفرستد. اکنون:
- بعد از لاگین موفق، یک توکن تصادفی ۱۶ بایتی ساخته می‌شود.
- فرانت‌اند آن را از `/api/session-token` (که خودش Basic Auth دارد) می‌گیرد و به‌عنوان اولین پیام می‌فرستد: `{"type":"auth","token":"..."}`
- تا این پیام تأیید نشود، هیچ `command`ای پذیرفته نمی‌شود.
- توکن بعد از ۱۵ دقیقه بی‌فعالیتی منقضی می‌شود (`SESSION_TOKEN_TIMEOUT`).

> ⚠️ این یک لایه‌ی دفاعی سبک و مناسب دستگاه embedded است، نه جایگزین HTTPS. بدون HTTPS خود توکن روی شبکه رمزنگاری نمی‌شود (همچنان در v2.1 باز است — به فهرست پایین صفحه مراجعه کنید).

**۲. رمزهای پیش‌فرض و صفحه‌ی تغییر رمز**
- پرچم `forcePasswordChange` در اولین بوت `true` است؛ `isUsingDefaultPassword()` آن را چک می‌کند و `main.cpp` هم در Serial و هم روی TFT هشدار نشان می‌دهد.
- `setWebPassword()` فقط رمزی را می‌پذیرد که حداقل ۸ کاراکتر و متفاوت از رمز پیش‌فرض (`"cartouch"`) باشد.
- **وب:** فرم تغییر رمز در تب تنظیمات (`/api/change-password`)؛ بعد از تغییر، session قبلی باطل می‌شود و اگر رمز هنوز پیش‌فرض باشد، هشدار قرمز نمایش داده می‌شود.
- **TFT:** دکمه‌ی «🔐 تغییر رمز ورود» در تب تنظیمات، با صفحه‌ی دو فیلدی و کیبورد مجازی LVGL (کیبورد فقط با فوکوس فیلد نمایش داده می‌شود). تغییر نام کاربری فقط از وب ممکن است.
- **(v2.1)** اکنون تغییر رمز از هر دو مسیر (TFT و وب) session های هر دو طرف را همزمان باطل می‌کند — نگاه کنید به بخش v2.1 بالا.

**۳. دور زدن احراز هویت در فایل‌های استاتیک**
`serveStatic("/", SPIFFS, "/")` هر فایل SPIFFS (از جمله `index.html`) را بدون `_authenticate()` سرو می‌کرد. با روت‌های صریح فقط برای `app.js` و `style.css` (غیرحساس) جایگزین شد؛ بقیه پشت احراز هویت ماند.

**۴. رمز خام روی Serial**
پرینت رمز وب در هر بوت حذف شد (فقط username چاپ می‌شود).

**۵. Rate Limiting** — دو لایه:
- `webserver.cpp`: هر کلاینت WebSocket حداکثر هر ۳۰۰ms یک فرمان.
- `vehicle_control.cpp`: صرف‌نظر از منبع فرمان، حداکثر هر ۱۵۰ms یک فرمان فیزیکی روی CAN ارسال می‌شود (جلوگیری از فشار مکانیکی به موتور شیشه/قفل).

### 🐛 باگ‌های رفع‌شده

| مشکل | فایل | رفع |
|---|---|---|
| **سرریز بافر CAN:** `data_length_code` بدون بررسی در حلقه‌ی کپی استفاده می‌شد و مقدار بزرگ‌تر از ۸ حافظه‌ی بیرون از `msg.data[8]` را خراب می‌کرد | `can_manager.cpp` | مقدار قبل از کپی به ۸ محدود می‌شود |
| **مصرف RAM (بحرانی):** `MAX_DBC_SIGNALS=200` آرایه‌ی `VehicleDB` را حدود **۱٫۲۲ مگابایت** می‌کرد (اندازه‌گیری با g++) و روی ESP32-S3 با ~۵۱۲KB SRAM بوت/لینک را خراب می‌کرد | `vehicle_db.h` | کاهش به ۲۴ (و در v2.1، همراه با افزایش سقف پیام، خود آبجکت به PSRAM منتقل شد) |
| **پارسر DBC (بحرانی):** فرمت `sscanf` خط `BO_` یک فاصله‌ی اضافه قبل از `:` داشت؛ چون هیچ‌کدام از فایل‌های DBC چنین فاصله‌ای ندارند، **هیچ پیامی هرگز پارس نمی‌شد** | `vehicle_db.cpp` | فرمت اصلاح و با `sscanf` واقعی روی همه‌ی فایل‌های DBC آزمایش شد |
| **سرریز CAN ID:** چند فایل واقعی (GM، Ford) از ID گسترده‌ی بالاتر از `INT_MAX` استفاده می‌کنند و `int` + `%d` آن‌ها را منفی می‌کرد | `vehicle_db.cpp` | تغییر به `uint32_t` + `%u` |
| **اندیس منفی:** `_parseSignalLine()` فرض می‌کرد قبلش همیشه یک `BO_` معتبر پارس شده | `vehicle_db.cpp` | بررسی مستقل اضافه شد |
| **خط طولانی DBC:** خط بلندتر از ۱۲۷ کاراکتر بی‌صدا بریده و اشتباه پارس می‌شد | `vehicle_db.cpp` | هشدار در Serial |
| **بریده شدن فایل بزرگ:** رسیدن به سقف `MAX_DBC_MESSAGES` کاملاً بی‌صدا بود | `vehicle_db.cpp` | هشدار truncation |
| **فایل‌های ناموجود:** لیست خودروها به `toyota_camry.dbc`، `honda_civic.dbc`، `bmw_3series.dbc`، `generic_obd2.dbc` اشاره می‌کرد که در `data/dbc` وجود نداشتند و بارگذاری بی‌صدا شکست می‌خورد | `vehicle_db.cpp` | اشاره به فایل‌های واقعی؛ «Generic / OBD-II» بدون DBC تعریف شد (و در v2.1 به ۳۸ مدل گسترش یافت) |

---

## 🚧 مشکلات شناخته‌شده و کارهای باقی‌مانده

وضعیت هر مورد با کد فعلی (بعد از v2.1) تطبیق داده شده است. فهرست
کامل‌تر و قابل ردیابی‌تر در
[`PROGRESS_CHECKLIST.md`](./PROGRESS_CHECKLIST.md) نگه داشته می‌شود.

### 🔴 مهم برای استفاده‌ی واقعی

| # | موضوع | جزئیات |
|:-:|---|---|
| 1 | **فرمان از DBC ممکن نیست** | برای خودروهای انتخاب‌شده از لیست DBC، `resolveCommand` عمداً پیام خطا برمی‌گرداند (اکثر DBC های OpenDBC فقط سیگنال «خواندن» دارند). فرمان فقط از Learn Mode یا ورود دستی می‌آید. این یک **محدودیت طراحی ذاتی** است، نه باگ — قابل رفع با کدنویسی بیشتر نیست |
| 2 | **Listen-Only در زمان اجرا فقط نرم‌افزاری است** | درایور TWAI فقط در `begin()` با حالت ذخیره‌شده نصب می‌شود؛ تغییر حالت در حین کار فقط فیلتر نرم‌افزاری را عوض می‌کند. تضمین سخت‌افزاری واقعی نیاز به متد «reconfigure سریع» در `CANManager` دارد که هنوز پیاده نشده |
| 3 | **بدون HTTPS** | ترافیک وب و توکن نشست رمزنگاری نمی‌شود |
| 4 | **بدون Secure Boot / Flash Encryption** | شروع نشده |
| 5 | **بدون device provisioning/pairing امن** | شروع نشده |
| 6 | **بدون auto-detect پروتکل قدیمی‌تر (ISO9141/KWP2000)** | فقط CAN 500Kbps پشتیبانی می‌شود |
| 7 | **بدون گواهی EMC/الزامات قانونی خودرو** | نیاز به آزمایشگاه گواهی رسمی — خارج از محدوده‌ی کدنویسی |
| 8 | **بدون error logging/telemetry برای دیباگ میدانی** | شروع نشده |
| 9 | **مسئولیت حقوقی/گارانتی مشخص نیست** | نیاز به مشاوره‌ی حقوقی — خارج از محدوده‌ی کدنویسی |
| 10 | **بدون تست میدانی روی fleet واقعی** | تمام اصلاحات این نسخه فقط به‌صورت دستی/منطقی بررسی شده‌اند |

### 🟡 محدودیت‌های فنی باقی‌مانده

| # | موضوع | جزئیات |
|:-:|---|---|
| 11 | **چند فایل DBC بسیار حجیم هنوز وصل نشده‌اند** | `ford_lincoln_base_pt.dbc`, `vw_mlb.dbc`, `vw_mqbevo.dbc`, `FORD_CADS*.dbc` — با سقف فعلی `MAX_DBC_MESSAGES=150` احتمالاً truncate می‌شوند. جزئیات در کامنت‌های `VehicleDB::begin()` |
| 12 | **فایل‌های DBC که نیاز به merge چندفایلی دارند** | چند فایل مکمل GM (`gm_global_a_high_voltage_management.dbc` و…) باید کنار فایل اصلی merge شوند؛ پارسر فعلی فقط یک فایل در هر انتخاب خودرو بارگذاری می‌کند |
| 13 | **Duty-cycle مکانیکی فقط rate-limit ساده است** | `MIN_COMMAND_INTERVAL_MS=150ms` هست ولی شمارشگر تجمعی/سایش موتور شیشه یا قفل پیاده نشده |
| 14 | **حجم SPIFFS** | `data/` حدود ۳٫۵ مگابایت است؛ روی پارتیشن `default_16MB.csv` جا می‌شود ولی با افزودن مدل‌های بیشتر ممکن است لازم به تنظیم پارتیشن سفارشی شود |

### 💡 پیشنهادهای توسعه

- شبیه‌ساز CAN روی PC برای تست state machine بدون خودرو
- تست واحد منطق diff/candidate-ranking با داده‌ی شبیه‌سازی‌شده (با g++ خالص)
- وصل کردن باقی فایل‌های DBC (نگاه کنید به بند ۱۱ و ۱۲ بالا)
- HTTPS با گواهی self-signed برای رفع بند ۳ بالا

### ✅ رفع‌شده (تجمعی، همه‌ی نسخه‌ها)

- `vehicle_control.cpp` دیگر CAN ID حدسی هاردکد را مسیر اصلی نمی‌کند؛ در v2.0 از `ActiveProfileManager` می‌خواند.
- پوشه‌ی `generator/` (اسکریپت‌های پایتون OpenDBC) دیگر در `data/dbc` نیست.
- تمام موارد فهرست‌شده در بخش «v2.1» بالای همین فایل.

</div>
