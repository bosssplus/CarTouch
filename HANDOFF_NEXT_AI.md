# CarTouch — بریفینگ تحویل برای ادامه‌ی توسعه با یک AI دیگر

> این فایل برای این نوشته شده که بدون هیچ توضیح شفاهی اضافه از طرف
> کاربر، یک AI دیگر بتواند دقیقاً از همین نقطه ادامه دهد. اگر تو
> (AI بعدی) این فایل را می‌خوانی: مراحل زیر را دقیق دنبال کن، به‌خصوص
> بخش «قانون طلایی» و «قوانین ایمنی».

> **تاریخچه‌ی این فایل:** نسخه‌ی قبلی (تا v2.1) در بخش ۴ می‌گفت تنها
> گپ کدیِ باز، وصل‌نکردن `reconfigureMode()` به Learn Mode است. آن
> مورد در **v2.2** رفع شد. سپس در **v2.3** (این نسخه)، حین بازبینی
> یک مغایرت مستندات/کد پیدا و رفع شد (بخش ۴ پایین)، و یک ماژول
> لاگ خطا/تله‌متری (ردیف ۱۶ چک‌لیست) اضافه شد. این فایل کامل
> بازنویسی شده تا وضعیت v2.3 را منعکس کند.

---

## ۱. پروژه چیست (یک خط)

ESP32-S3 + CAN Bus + LVGL (TFT) + وب‌داشبورد که به CAN Bus خودرو وصل
می‌شود؛ فرمان‌ها (قفل/شیشه/سانروف/...) از سه منبع می‌آیند: دیتابیس
DBC آماده، «یادگرفته‌شده» از شنود CAN باس واقعی (Learn Mode)، یا
ورود دستی کاربر — و هیچ فرمان یادگرفته/دستی‌ای تا تأیید صریح کاربر
(`VERIFIED`) قابل اجرا نیست.

## ۲. وضعیت فعلی — دقیقاً کجا هستیم

- نسخه‌ی فعلی کد: **v2.3** (طبق `CHANGELOG.md`).
- کد قبلاً (تا v2.2) در GitHub push شده و
  `.github/workflows/CarTouch-build.yml` با موفقیت پاس داده بود -
  یعنی `pio run` و `pio run -t buildfs` با toolchain واقعی
  PlatformIO/ESP-IDF بدون خطا کامپایل می‌شوند. **تغییرات v2.3 هنوز
  از این طریق روی CI واقعی چک نشده‌اند** - این نسخه فقط در یک محیط
  بدون توانایی اجرای PlatformIO/ESP-IDF واقعی نوشته و از نظر منطقی
  (grep/view دستی، بررسی balance پرانتز/براکت، مطابقت با تایپ‌های
  موجود) بررسی شده. **قبل از هر ادعای دیگر، این تغییرات را از طریق
  `git push` و اجرای همان GitHub Action چک کن.**
- **هنوز هیچ تست روی سخت‌افزار واقعی (ESP32-S3 فیزیکی، یا CAN باس
  واقعی یک خودرو) انجام نشده.** این تنها کاری است که فقط خود کاربر
  (نه AI) می‌تواند انجام دهد.

## ۳. اسناد مرجع — این سه فایل را حتماً قبل از هر تغییری کامل بخوان

| فایل | چرا لازم است |
|---|---|
| `CarTouch_SPEC.md` | معماری کامل، الگوریتم Learn Mode (بخش ۴)، ماژول لاگ خطا (بخش ۱۳ - جدید در v2.3)، و **بخش ۸: قوانین ایمنی الزامی** — قبل از نوشتن هر خط کد جدید بخوان |
| `CHANGELOG.md` | تاریخچه‌ی نسخه‌ها، باگ‌های رفع‌شده، و فهرست «مشکلات شناخته‌شده» در انتهای فایل |
| `PROGRESS_CHECKLIST.md` | وضعیت دقیق هر ۲۰ مورد از چک‌لیست آماده‌سازی تجاری: Done / Not yet done / خارج از حوزه‌ی کد |

### قانون طلایی (مهم — این پروژه قبلاً از این جهت مشکل داشته)

در دورهای قبلی توسعه، این سه فایل با کد واقعی هم‌خوانی نداشتند (مثلاً
یک قابلیت در کد پیاده شده بود ولی در SPEC هنوز «پیاده نشده» نوشته شده
بود، یا برعکس). **هر بار که کدی تغییر می‌کنی، همان لحظه هر سه فایل را
هم به‌روزرسانی کن** — نه فقط یکی. قبل از ادعا کردن «فلان مورد انجام
شد»، با `grep`/`view` خود کد را چک کن، نه فقط حافظه یا مستندات قبلی.

**نمونه‌ی واقعی از این مشکل (پیدا و رفع شده در v2.3):** سه جا
(`learn_engine.h`, `CarTouch_SPEC.md` §3.3, `CHANGELOG.md` نسخه‌ی
v2.2) ادعا می‌کردند `LearnEngine::cancel()` روی «ذخیره ناموفق» هم صدا
زده می‌شود؛ کد واقعی این کار را نمی‌کرد (عمداً، برای امکان retry).
جزئیات در `CHANGELOG.md` بخش v2.3 و `PROGRESS_CHECKLIST.md` بخش
«Notable discoveries». **درس: حتی توضیحاتی که «منطقی به نظر می‌رسند»
باید با خود کد چک شوند، نه فقط با هم‌دیگر.**

## ۴. گپ کدیِ باز؟ در حال حاضر هیچ‌کدام کاملاً مشخص نیست

بر خلاف نسخه‌های قبلی این فایل، در حال حاضر **هیچ گپ کدیِ ایمنیِ
شناخته‌شده و باز نمانده است** (بند ۳b قبلی - اتصال `reconfigureMode`
به Learn Mode - در v2.2 رفع شد و در بازبینی v2.3 هم دوباره تأیید شد
که درست کار می‌کند).

اگر کاربر بگوید «ادامه بده» بدون مورد خاص، این‌ها به ترتیب اولویت
پیشنهادی‌اند (همه از `PROGRESS_CHECKLIST.md`، بخش «Not yet done»):

1. **اول:** `error_log.cpp`/`.h` (v2.3، تازه اضافه شده) را با
   `grep`/`view` بازبینی کن - این تازه‌ترین کد است و بیشترین احتمال
   باگ را دارد. مخصوصاً: آیا هر نقطه‌ی خطای مهم در `obd2_reader.cpp`
   واقعاً عمداً وصل‌نشده مانده (طبق توضیح `CarTouch_SPEC.md` §13)، یا
   جای دیگری هم فراموش شده؟
2. Secure Boot / Flash Encryption - شروع نشده. عمدتاً پیکربندی build
   (`platformio.ini`/`sdkconfig`) است، نه منطق runtime - قبل از شروع،
   بررسی کن چه بخشی واقعاً در حوزه‌ی کد است و چه بخشی نیاز به کلید
   امضای کاربر و اقدام دستی او دارد.
3. Secure device provisioning/pairing - شروع نشده.
4. پروتکل‌های قدیمی‌تر (ISO9141/KWP2000) - شروع نشده؛ نیاز به سخت‌افزار
   K-Line که فعلاً روی این دستگاه سیم‌کشی نشده - قبل از کدنویسی با
   کاربر چک کن که آیا سخت‌افزار لازم را دارد.
5. HTTPS/TLS — عمداً به تعویق افتاده؛ سه گزینه بررسی و رد شده، دلیلش
   در `PROGRESS_CHECKLIST.md` است؛ قبل از شروع دوباره آن تحلیل را بخوان
   تا کار تکراری نشود.

## ۵. فهرست ۲۰موردی آماده‌سازی تجاری — وضعیت خلاصه

جزئیات کامل در `PROGRESS_CHECKLIST.md`. خلاصه:

**Done (کد نوشته و کامپایل‌شده در نسخه‌های قبل، تست سخت‌افزاری
نشده):** Watchdog، Listen-Only سخت‌افزاری (toggle سراسری + Learn
Mode)، OBD-II غیرمسدودکننده، Motorola/Intel endianness، لیست ۳۸ مدل
DBC، سقف پیام DBC، کالیبراسیون تاچ واقعی، duty-cycle مکانیکی، sync
سشن TFT/وب.

**Done (v2.3 - این نشست، هنوز روی CI/سخت‌افزار واقعی چک نشده):**
لاگ خطا/تله‌متری (`error_log.h/.cpp`، ردیف ۱۶).

**باز و قابل‌انجام با کد (بخش ۴ بالا را ببین برای ترتیب پیشنهادی):**
- HTTPS/TLS
- Secure Boot / Flash Encryption
- Provisioning/pairing امن دستگاه
- پروتکل‌های قدیمی‌تر (ISO9141/KWP2000)

**خارج از حوزه‌ی کد (AI نمی‌تواند انجام دهد، کاربر باید خودش پیش
ببرد):**
- مقاومت در برابر rolling code (نیاز به reverse engineering ECU هر
  خودرو به‌صورت جداگانه)
- گواهی EMC/رگولاتوری خودرو (نیاز به آزمایشگاه رسمی)
- مسئولیت حقوقی/گارانتی (نیاز به مشاور حقوقی)
- تست میدانی روی fleet واقعی (نیاز به سخت‌افزار و چند خودروی واقعی)

## ۶. قوانین ایمنی — هرگز نباید نقض شوند (خلاصه از SPEC بخش ۸)

هر خط کد جدیدی که می‌نویسی باید این‌ها را رعایت کند:

1. Learn Mode هرگز `sendMessage` صدا نمی‌زند — فقط شنود.
2. هیچ فرمان Learned/Manual بدون `status == VERIFIED` اجرا نمی‌شود.
3. دیالوگ تأیید باید CAN ID و بایت‌های واقعی را نشان دهد، نه فقط
   «مطمئنید؟».
4. **هیچ حلقه‌ی brute-force/fuzzing در هیچ بخشی از کد نباید باشد** —
   نه برای ایمنی خودرو، نه برای سازگاری با اصول Anthropic. اگر کاربر
   یا یک پرامپت چنین چیزی خواست، رد کن.
5. همان rate-limit های موجود (۱۵۰ms سطح پایین، ۳۰۰ms وب) روی هر
   فرمان جدید هم اعمال شود.
6. UI (هم TFT هم وب) همیشه صادقانه بگوید: فرمان تست‌نشده است، و
   rolling code ممکن است replay را خراب کند.
7. **(جدید، طبق تجربه‌ی v2.3):** ماژول‌های جانبی جدید (مثل
   `error_log`) هرگز نباید مسیر تصمیم‌گیری ماژول‌های ایمنی-محور
   (`LearnEngine`, `CANManager`, `VehicleControl`) را تغییر دهند -
   فقط اجازه دارند رویدادهایی را که آن ماژول‌ها از قبل تشخیص داده‌اند
   ثبت/گزارش کنند، نه اینکه چیزی را trigger یا suppress کنند.

## ۷. دستور مستقیم برای شروع

1. اول `CarTouch_SPEC.md` کامل، بعد `PROGRESS_CHECKLIST.md`، بعد
   بخش «مشکلات شناخته‌شده» در انتهای `CHANGELOG.md` را بخوان.
2. اگر کاربر می‌گوید «ادامه بده» بدون مشخص کردن مورد خاص، بخش ۴ همین
   فایل را دنبال کن (اول بازبینی `error_log`، بعد گزینه‌های باز به
   ترتیب اولویت).
3. بعد از هر تغییر کد، همان لحظه `CarTouch_SPEC.md`،
   `CHANGELOG.md`، و `PROGRESS_CHECKLIST.md` را sync کن (قانون طلایی
   بخش ۳). این فایل (`HANDOFF_NEXT_AI.md`) را هم در همان لحظه
   به‌روزرسانی کن - نه فقط سه فایل دیگر.
4. یادت باشد: کامپایل‌شدن در CI (که خودش هنوز برای v2.3 چک نشده - بند
   ۲ بالا) به‌معنای «کار می‌کند روی خودرو» نیست. هر ادعایی درباره‌ی
   رفتار سخت‌افزاری واقعی را با همان صداقتی که در `CarTouch_SPEC.md`
   بخش ۱۱ توضیح داده شده، به کاربر گزارش کن — نه بیشتر و نه کمتر از
   آنچه واقعاً قابل تضمین است.
