# CarTouch v2.0 — مشخصات کامل پروژه (Project Specification)

> این فایل یک سند مرجع کامل و خودکفا است. هدف: اگر توسعه‌ی این پروژه با
> هر ابزار یا هوش مصنوعی دیگری ادامه پیدا کند، همین یک فایل کافی باشد
> تا معماری، تصمیمات طراحی، محدودیت‌های ایمنی و مسیر باقی‌مانده کاملاً
> روشن باشد — بدون نیاز به مکالمه‌ی قبلی.

> 🔗 اسناد مرتبط: [`README.md`](./README.md) (معرفی و راه‌اندازی) ·
> [`CHANGELOG.md`](./CHANGELOG.md) (تغییرات و مشکلات شناخته‌شده)

**فهرست:**
۱. زمینه و چرایی طراحی · ۲. اصل «هیچ قابلیتی حذف نمی‌شود» ·
۳. معماری جدید · ۴. Learn Mode (الگوریتم) · ۵. ورود دستی ·
۶. ذخیره‌سازی دائمی · ۷. تغییرات فایل‌به‌فایل · ۸. قوانین ایمنی ·
۹. متن‌های صادقانه‌ی UI · ۱۰. ترتیب توسعه · ۱۱. تست ·
۱۲. ادامه‌ی مسیر · ۱۳. خلاصه‌ی یک‌خطی

**تاریخ نگارش سند:** ۲۰ سپتامبر ۲۰۲۶
**نسخه پایه:** CarTouch v1.0 (ESP32-S3 + CAN Bus + LVGL + Web Dashboard)
**هدف این نسخه:** افزودن قابلیت "Learn Mode" (یادگیری فرمان از روی
CAN Bus واقعی)، دیتابیس دستی کاربر، و حفظ کامل دیتابیس DBC آماده —
بدون حذف هیچ قابلیت موجود.

---

## ۱. زمینه و چرا این پروژه به این شکل طراحی شده

### ۱.۱ خواسته‌ی اصلی کاربر (عیناً خلاصه‌شده)

کاربر یک دستگاه ESP32-S3 می‌خواهد که:
1. به CAN Bus هر خودرویی وصل شود (از طریق OBD-II یا مستقیم)
2. **اگر مدل خودرو مشخص و در دیتابیس DBC موجود باشد**، از همان
   استفاده کند
3. **اگر مدل مشخص نیست یا در دیتابیس نیست**، یک "حالت یادگیری"
   داشته باشد: کاربر یک‌بار کنار خودرو با دستگاه فیزیکی کار می‌کند،
   هر دکمه واقعی خودرو (قفل، شیشه، ...) را می‌زند، دستگاه پیام‌های
   CAN مربوطه را ضبط و ذخیره می‌کند
4. **گزینه‌ی دستی**: کاربر بتواند خودش CAN ID و بایت‌ها را (از منبع
   دیگری مثل فروم/مستندات) مستقیماً وارد کند
5. بعد از یادگیری/وارد کردن، بتواند از راه دور (وب یا TFT) همان
   فرمان‌ها را دوباره اجرا (replay) کند
6. بتواند نتیجه را به اسم دلخواه (مثلاً "پراید بابا") ذخیره و بعداً
   دوباره انتخاب کند
7. **ایمنی**: دستگاه همیشه اول در حالت خواندن/شنود (Listen-Only)
   شروع کند، هیچ فرمانی به باس نفرستد، تا کاربر صریحاً تأیید کند که
   می‌خواهد وارد "حالت اقدام" (Active) شود

### ۱.۲ محدودیت‌های فیزیکی/ایمنی که این طراحی را شکل داده‌اند
(این بخش برای AI/توسعه‌دهنده‌ی بعدی حیاتی است — نادیده گرفتنش خطرناک است)

- **کشف کاملاً خودکار معنای پیام‌های CAN ممکن نیست و امن نیست.**
  هیچ الگوریتمی نمی‌تواند با اطمینان ۱۰۰٪ بگوید یک پیام CAN یعنی
  "قفل درب" — فقط می‌توان با diff گرفتن (قبل/بعد از فشردن دکمه واقعی)
  کاندید پیدا کرد. تأیید نهایی همیشه باید دستی و توسط انسان باشد.
- **Replay کردن پیام‌های ضبط‌شده تضمین‌شده نیست.** خودروهای جدیدتر
  (بعد از ~۲۰۱۸، به‌خصوص پریمیوم) از rolling code/counter/checksum
  استفاده می‌کنند که با replay ساده کار نمی‌کند. این محدودیت باید
  **در UI به‌صراحت به کاربر گفته شود**، نه پنهان شود.
- **دیتابیس‌های DBC عمومی (OpenDBC) عمدتاً داده‌ی "خواندن" دارند،
  نه "نوشتن".** یعنی حتی وقتی مدل خودرو در دیتابیس DBC است، ممکن
  است RPM/سرعت را بتوانیم بخوانیم ولی فرمان قفل/شیشه در آن فایل
  اصلاً تعریف نشده باشد (چون خودروسازها این بخش را مخفی نگه
  می‌دارند). به همین دلیل Learn Mode و Manual DB لازم است، حتی برای
  خودروهایی که در DBC هستند.
- **دستگاه نباید خودش به‌صورت خودکار روی باس زنده‌ی خودرو فرمان
  آزمایشی/تصادفی بفرستد** (نه در حالت fuzzing، نه در "کشف خودکار").
  این می‌تواند سیستم‌های ایمنی (ترمز، ایربگ، فرمان) را غیرقابل‌پیش‌بینی
  تحت تأثیر قرار دهد. هر ارسال فرمان باید نتیجه‌ی یک تأیید صریح
  انسانی باشد.

---

## ۲. اصل راهنما: هیچ قابلیت موجود حذف نمی‌شود

نسخه‌ی v1.0 (که پایه‌ی این کار است) این قابلیت‌ها را داشت و **باید
همگی دقیقاً به همان شکل باقی بمانند**:

- کنترل لمسی TFT (۳ تب: کنترل، داشبورد، تنظیمات) با LVGL 8
- Web Dashboard با WebSocket + session token auth
- خواندن OBD-II استاندارد (RPM، سرعت، دما، دریچه گاز، سوخت، runtime، DTC)
- پارسر DBC (BO_/SG_/CM_/VAL_) برای فایل‌های موجود در `data/dbc/`
  (۵۷ فایل واقعی OpenDBC)
- لیست انتخاب خودرو از DBC (Generic/OBD-II, Toyota Prius 2010,
  Toyota 2017 Ref PT, BMW E9x/E8x — طبق `vehicle_db.cpp`)
- کنترل درب (قفل/باز)، شیشه‌ها (بالا/پایین، تکی/همه)، سانروف، صندوق،
  آینه، دزدگیر
- حالت Listen-Only سراسری (در `AppConfig.listenOnlyMode`)
- Auto Sleep بعد از ۱۰ دقیقه + بیداری با پیام CAN
- حالت شب/روز خودکار/دستی
- امنیت: session token برای WebSocket، rate-limit (۳۰۰ms وب،
  ۱۵۰ms سطح پایین در vehicle_control)، اجبار تغییر رمز پیش‌فرض،
  صفحه تغییر رمز هم در TFT هم در وب
- تمام رفع‌باگ‌های قبلی: RAM overflow در DBC signals، CAN buffer
  overflow، پارسر BO_/negative index، auth bypass در static files

هر فایل جدید/تغییریافته باید با این چک‌لیست تطبیق داده شود.

---

## ۳. معماری جدید — نمای کلی

### ۳.۱ سه منبع فرمان (Command Sources)

سیستم کنترل خودرو (`vehicle_control`) دیگر مستقیماً CAN ID هاردکد
ندارد. به‌جایش از یک **"پروفایل فرمان فعال" (Active Command Profile)**
می‌خواند که می‌تواند از سه منبع باشد:

| منبع | کلاس/فایل مسئول | چه زمانی |
|---|---|---|
| **DBC آماده** | `VehicleDB` (موجود، بدون تغییر ساختاری) | مدل دقیق شناخته‌شده و در `data/dbc/*.dbc` هست |
| **یادگرفته‌شده (Learned)** | `LearnEngine` + `CustomVehicleStore` (جدید) | Learn Mode با دکمه فیزیکی واقعی |
| **دستی (Manual)** | `CustomVehicleStore` (جدید، همان ساختار Learned) | کاربر مستقیم CAN ID/بایت وارد می‌کند |

هر سه در یک ساختار مشترک `CommandProfile` یکسان دیده می‌شوند تا
`vehicle_control.cpp` با هر سه یکسان کار کند (polymorphism ساده با
یک enum `source`، نه class hierarchy پیچیده — برای سادگی روی MCU).

### ۳.۲ جریان کاربر (User Flow) — خلاصه

```
[شروع] → انتخاب خودرو
           ├─ از لیست DBC آماده (مثل قبل، بدون تغییر)
           ├─ از لیست پروفایل‌های ذخیره‌شده‌ی من (Learned/Manual قبلی)
           └─ ساخت پروفایل جدید
                ├─ حالت یادگیری (Learn Mode) → می‌رود به بخش ۴
                └─ ورود دستی (Manual Entry) → می‌رود به بخش ۵

[بعد از داشتن فرمان‌ها] → هر فرمان با وضعیت:
                            UNVERIFIED (⚠️ زرد) یا VERIFIED (✅ سبز)
                          → کاربر باید صریحاً "تأیید کن" بزند
                          → فقط VERIFIED در تب کنترل اصلی قابل اجراست
```

### ۳.۳ حالت‌های سراسری دستگاه (State Machine اصلی)

این state machine جایگزین/تکمیل‌کننده‌ی `DeviceMode` موجود
(`MODE_LISTEN_ONLY/MODE_ACTIVE/MODE_SLEEP/MODE_DEEP_SLEEP`) در
`config.h` است — همان enum حفظ می‌شود، فقط یک لایه بالاتر برای
Learn Mode اضافه می‌شود:

```
DeviceMode (موجود، بدون تغییر):
  MODE_LISTEN_ONLY / MODE_ACTIVE / MODE_SLEEP / MODE_DEEP_SLEEP

LearnModeState (جدید، مستقل و موازی):
  LEARN_IDLE
    → LEARN_BASELINE_CAPTURE   (۲ ثانیه پیش‌زمینه، هیچ فرمانی به باس نمی‌رود)
    → LEARN_WAITING_ACTION     (به کاربر می‌گوید: الان دکمه واقعی رو بزن)
    → LEARN_ACTION_CAPTURE     (۲ ثانیه بعدی، تفاوت با baseline محاسبه می‌شود)
    → LEARN_CANDIDATES_READY   (کاندیدها نمایش داده می‌شوند)
    → LEARN_NAMING             (کاربر نام دلخواه می‌دهد)
    → LEARN_SAVED_UNVERIFIED   (ذخیره شد، ولی هنوز status=UNVERIFIED)
  [کاربر جدا و صریح]:
    UNVERIFIED --"تأیید کن"--> VERIFIED  (تنها اینجا اجازه‌ی اجرا در تب کنترل داده می‌شود)
```

**قانون سخت‌گیرانه:** در تمام مراحل `LEARN_*` به جز لحظه‌ی تأیید نهایی،
`CANManager` باید در حالت واقعی Listen-Only باشد (نه فقط چک منطقی —
اگر `twai_start()` در حالت `TWAI_MODE_LISTEN_ONLY` نصب شده، حتی در
صورت باگ نرم‌افزاری هم چیزی فیزیکی فرستاده نمی‌شود). این یعنی هنگام
ورود به Learn Mode، دستگاه ممکن است لازم باشد CAN driver را مجدداً
با `TWAI_MODE_LISTEN_ONLY` نصب کند (چون TWAI mode بعد از `begin()`
در ESP-IDF قابل تغییر runtime نیست؛ باید `end()` سپس `begin()` با
فلگ متفاوت).

> **وضعیت پیاده‌سازی (به‌روزرسانی v2.2 — صادقانه گزارش‌شده):**
> متد `CANManager::reconfigureMode(bool listenOnly)` در v2.1 اضافه
> شد و واقعاً `twai_stop()` + `twai_driver_uninstall()` + نصب مجدد
> با `TWAI_MODE_LISTEN_ONLY`/`TWAI_MODE_NORMAL` انجام می‌دهد (نه فقط
> یک فلگ نرم‌افزاری). در v2.1 این متد فقط به فرمان سراسری «حالت
> شنود/فعال» وصل بود. **در v2.2 به نقاط ورود Learn Mode هم وصل شد:**
> `LearnEngine::beginLearning()` اکنون، قبل از هر چیز، اگر درایور از
> قبل در حالت شنود واقعی نباشد، `canManager.reconfigureMode(true)`
> را صدا می‌زند و حالت قبلی را به خاطر می‌سپارد (گزینه‌ی «الف» که
> در این بخش پیشنهاد شده بود — پذیرفتن تأخیر uninstall/install در
> همان ابتدا، پیش از شروع پنجره‌ی زمان‌حساس، نه وسط آن).
> `LearnEngine::startBaselineCapture()` و `::confirmReadyForAction()`
> هر دو، به‌عنوان دفاع در عمق، قبل از باز کردن پنجره‌ی capture دوباره
> `canManager.isListenOnlyActive()` را چک می‌کنند و در صورت نبودن
> حالت شنود واقعی، به‌جای ادامه دادن، وارد `LEARN_ERROR` می‌شوند.
> `LearnEngine::cancel()` — که در «ذخیره موفق» و «لغو صریح» صدا زده
> می‌شود (در `webserver.cpp` و `tft_ui.cpp`) — حالت قبلی درایور را
> برمی‌گرداند. **اصلاحیه (v2.3، طبق قانون طلایی):** عمداً در مسیر
> «ذخیره ناموفق» (مثلاً پروفایلی انتخاب نشده، یا خطای فضای ذخیره‌سازی)
> صدا زده *نمی‌شود*، تا کاربر بتواند کاندید یا پروفایل دیگری انتخاب
> کند و بدون نیاز به capture مجدد دوباره تلاش کند؛ این تغییری در
> ایمنی نیست، چون ماندن در Listen-Only اجباری برای مدت بیشتر همیشه
> جهت ایمن است، نه جهت ناایمن. اگر بازگرداندن به حالت Normal شکست
> بخورد، دستگاه عمداً در Listen-Only باقی می‌ماند (fail-safe، نه
> fail-open). گزینه‌ی «ب»
> (یک متد «reconfigure سریع» بدون uninstall/install کامل) همچنان
> به‌عنوان بهبود آینده در بخش ۱۲ باقی می‌ماند، ولی دیگر یک گپ ایمنی
> باز نیست — با گزینه‌ی «الف» تضمین سطح سخت‌افزار برقرار است. جزئیات
> کامل در `CHANGELOG.md` نسخه‌ی v2.2 و `PROGRESS_CHECKLIST.md` ردیف
> ۳.
>
> **مهم — این تضمین جدید، تضمین سطح کد قدیمی (`LearnEngine` هرگز
> `sendMessage` صدا نمی‌زند) را جایگزین نمی‌کند، بلکه لایه‌ی مستقل
> دومی روی آن است؛** هر دو باید هم‌زمان برقرار بمانند.

---

## ۴. Learn Mode — مشخصات دقیق فنی

### ۴.۱ الگوریتم Capture & Diff

```
1. کاربر یک برچسب انتخاب می‌کند (از لیست از پیش تعریف‌شده یا آزاد):
   پیشنهادهای پیش‌فرض: lock_all, unlock_all, window_fl_up, window_fl_down,
   window_fr_up, window_fr_down, window_rl_up, window_rl_down,
   window_rr_up, window_rr_down, all_windows_up, all_windows_down,
   sunroof_open, sunroof_close, trunk_open, mirror_fold, mirror_unfold,
   alarm_arm, alarm_disarm, custom (نام آزاد)

2. BASELINE_CAPTURE (پیش‌فرض 2000ms، قابل تنظیم):
   - flushRxQueue()
   - یک جدول هش ساده <canId, lastDataBytes[8], lastSeenCount> برای
     تمام پیام‌های دیده‌شده در این بازه نگه داشته می‌شود
     (حداکثر BASELINE_MAX_IDS = 64 ورودی، در PSRAM اگر ممکن)

3. WAITING_ACTION:
   - به کاربر در UI/وب نمایش داده می‌شود: "حالا دکمه [نام] رو در خودرو بزن"
   - دکمه "شروع ضبط" را کاربر می‌زند (نه خودکار) تا از قبل/بعد
     تصادفی جلوگیری شود

4. ACTION_CAPTURE (پیش‌فرض 2000ms، قابل تنظیم، حداکثر 5000ms):
   - همه‌ی پیام‌های این بازه با baseline مقایسه می‌شوند
   - یک پیام "کاندید" است اگر:
     (الف) CAN ID در baseline اصلاً دیده نشده بود، یا
     (ب) CAN ID بود ولی بایت‌های داده تغییر کردند
   - هر کاندید با شمارنده‌ی "چند بار در این بازه دیده شد" ذخیره می‌شود
   - حداکثر CANDIDATE_MAX = 10 کاندید (بیشترین‌ها بر اساس اولویت:
     تغییر بایت > پیام جدید کامل، چون خیلی از پیام‌های periodic دائم
     در حال تغییر جزئی‌اند مثل RPM/timestamp که باید فیلتر شوند)

5. فیلتر نویز (مهم برای کیفیت نتیجه):
   - پیام‌هایی که فرکانس‌شان در baseline خیلی بالا بود (>20 بار در ۲
     ثانیه — یعنی احتمالاً یک پیام دوره‌ای عادی مثل RPM/wheel speed
     است، نه نتیجه‌ی فشردن دکمه) با اولویت پایین‌تر (نه حذف کامل)
     نمایش داده می‌شوند — چون کاربر باید خودش با چشم تصمیم بگیرد

6. CANDIDATES_READY:
   - لیست به کاربر نمایش داده می‌شود: هر ردیف =
     "CAN ID (hex) | بایت‌ها (hex) | تعداد تکرار | نوع (جدید/تغییر)"
   - کاربر روی یکی می‌زند: "این همونه"
   - یا "هیچکدوم نبود، دوباره امتحان کن" → برمی‌گردد به مرحله ۳

7. ذخیره: یک LearnedCommand جدید با status=UNVERIFIED در
   CustomVehicleProfile فعلی/جدید ذخیره می‌شود.
```

### ۴.۲ تأیید (Verification) — قبل از اقدام واقعی

طبق خواسته‌ی صریح کاربر ("اول حالت خواندن، بعد که مطمئن شد بره تو
اقدام")، این مرحله جداست:

```
- در لیست فرمان‌های یک پروفایل، هر ردیف وضعیت UNVERIFIED (⚠️) دارد
- کاربر روی "تأیید و آماده‌سازی" می‌زند
- یک دیالوگ تأیید صریح نمایش داده می‌شود:
  "با اجرای این فرمان، پیام CAN زیر واقعاً روی باس خودرو فرستاده
   می‌شود: ID=0x123, Data=[..]. این عمل غیرقابل‌بازگشت است (مثلاً
   قفل باز می‌شود). ادامه می‌دهید؟" [بله] [خیر]
- در صورت "بله": دستگاه از حالت Listen-Only واقعی خارج شده (اگر
  فعال بود)، فرمان یک‌بار با rate-limit موجود (۱۵۰ms) ارسال می‌شود،
  و کاربر می‌بیند که آیا خودرو واکنش نشان داد یا نه (نظارت دستی —
  دستگاه نمی‌تواند خودش تشخیص دهد "درب باز شد" مگر یک DBC پیام
  وضعیت برای آن باشد)
- کاربر دستی تأیید می‌کند "درست کار کرد" → status=VERIFIED
- یا "کار نکرد" → status برمی‌گردد به UNVERIFIED با یک شمارنده‌ی
  failCount، و کاربر می‌تواند دوباره Learn Mode را برای همین برچسب
  تکرار کند (بازنویسی می‌شود)
```

فقط فرمان‌های `VERIFIED` در تب کنترل اصلی (دکمه‌های بزرگ قفل/شیشه/...)
فعال/کلیک‌پذیرند. فرمان‌های `UNVERIFIED` در تب کنترل به‌صورت
disabled/خاکستری با آیکون ⚠️ نمایش داده می‌شوند (نه اینکه اصلاً
دیده نشوند — کاربر باید بداند وجود دارند ولی هنوز تأیید نشده‌اند).

### ۴.۳ محدودیت‌های حافظه (طبق همان انضباط بررسی RAM ثبت‌شده در CHANGELOG.md)

```cpp
#define MAX_CUSTOM_VEHICLES        8   // تعداد پروفایل‌های دست‌ساز/یادگرفته
#define MAX_LEARNED_COMMANDS_PER_VEHICLE  32  // هر پروفایل حداکثر ۳۲ فرمان
#define BASELINE_MAX_IDS            64  // حداکثر CAN ID متمایز در baseline
#define CANDIDATE_MAX               10  // حداکثر کاندید نمایش‌داده‌شده
```

محاسبه‌ی حجم: هر `LearnedCommand` ≈ ۵۰ بایت (برچسب ۳۲ + CAN ID ۴ +
داده ۸ + متادیتا) → هر پروفایل ≈ ۳۲×۵۰ = ۱۶۰۰ بایت → ۸ پروفایل ≈
۱۲.۸ کیلوبایت. کاملاً در محدوده‌ی امن RAM (برخلاف باگ قبلی
MAX_DBC_SIGNALS=200 که ۱.۲ مگابایت می‌شد). اگر با PSRAM هم ذخیره شود
مشکلی نیست، ولی چون کوچک است می‌تواند در DRAM بماند؛ فقط باید در
NVS/SPIFFS هم persist شود تا بعد از ریبوت از دست نرود (بخش ۶).

---

## ۵. ورود دستی (Manual Database Entry)

فرم ساده (هم TFT هم وب) برای وقتی کاربر از منبع دیگری (فروم، ابزار
دیگر، مستندات) CAN ID و بایت‌ها را می‌داند:

فیلدها:
- برچسب (از همان لیست پیشنهادی بخش ۴.۱ یا آزاد)
- CAN ID (hex، ۱۱ یا ۲۹ بیتی — چک‌باکس Extended)
- طول داده (۰ تا ۸)
- بایت‌های داده (۸ فیلد hex، یا رشته‌ی hex مثل "01 FF 00 00 00 00 00 00")

ورودی دستی هم با `status=UNVERIFIED` ذخیره می‌شود و باید از همان
مسیر تأیید بخش ۴.۲ عبور کند — چون داده‌ی دستی حتی بیشتر از داده‌ی
یادگرفته‌شده مستعد خطای انسانی (اشتباه تایپی، منبع نامعتبر) است.

اعتبارسنجی ورودی:
- CAN ID: اگر Extended نیست باید ≤ 0x7FF باشد، اگر Extended باید
  ≤ 0x1FFFFFFF
- طول داده ≤ ۸
- هر بایت باید ۰۰-FF معتبر hex باشد

---

## ۶. ذخیره‌سازی دائمی (Persistence)

### ۶.۱ چرا NVS کافی نیست و باید SPIFFS/JSON استفاده شود

`AppConfig` فعلی یک struct ثابت است که مستقیم در NVS به‌صورت blob
ذخیره می‌شود (`nvs_set_blob`). این برای تنظیمات ثابت خوب است، ولی
پروفایل‌های سفارشی:
- تعدادشان متغیر است (۰ تا ۸)
- هر کدام تعداد متغیر فرمان دارند (۰ تا ۳۲)
- باید مستقل از `AppConfig` قابل export/import باشند (بخش ۶.۳)

**تصمیم معماری:** پروفایل‌های سفارشی در فایل‌های جداگانه‌ی JSON در
SPIFFS ذخیره می‌شوند، نه در NVS blob:

```
/custom_vehicles/profile_0.json
/custom_vehicles/profile_1.json
...
/custom_vehicles/index.json   (لیست خلاصه: id, name, brand, model, commandCount — برای نمایش سریع لیست بدون پارس همه فایل‌ها)
```

فرمت هر `profile_N.json`:
```json
{
  "id": 0,
  "name": "پراید بابا",
  "brand": "Saipa",
  "model": "Pride 141",
  "year": 2015,
  "source": "learned",
  "commands": [
    {
      "label": "lock_all",
      "displayName": "قفل همه درب‌ها",
      "canId": 291,
      "extended": false,
      "length": 1,
      "data": [1, 0, 0, 0, 0, 0, 0, 0],
      "status": "verified",
      "timesObserved": 3,
      "failCount": 0,
      "createdAt": 1234567
    }
  ]
}
```

از `ArduinoJson` (که پروژه از قبل به‌عنوان dependency دارد،
`bblanchon/ArduinoJson@^7.2.0`) استفاده می‌شود — بدون کتابخانه‌ی جدید.

### ۶.۲ محدودیت اندازه‌ی SPIFFS

پوشه‌ی `data/` (شامل ۵۷ فایل DBC) حدود ۳.۴ مگابایت است (پوشه‌ی
`generator/` قبلاً حذف شده؛ رجوع کنید به CHANGELOG.md). فلش ۱۶ مگابایتی
پروژه با `default_16MB.csv` پارتیشن‌بندی می‌شود، ولی پارتیشن SPIFFS در
آن فقط بخشی از ۱۶MB است (طبق نسخه‌ی استاندارد Arduino-ESP32 حدود
۳.۴MB — با `buildfs` تأیید شود). یعنی فضای آزاد SPIFFS برای پروفایل‌ها
ممکن است کم باشد و در صورت لزوم باید DBC های غیرضروری حذف شوند.
خود پروفایل‌های JSON ناچیزند: `MAX_CUSTOM_VEHICLES=8` × حداکثر ~۴KB هر
فایل ≈ ۳۲KB.

### ۶.۳ Export/Import (توصیه‌شده، نه الزامی برای MVP)

چون کاربر گفته "بتونم داده‌ها رو برای انجام واقعی خودم ذخیره کنه":
- دکمه‌ی "دانلود پروفایل" در وب: فایل JSON پروفایل را مستقیم دانلود
  می‌کند (از طریق یک endpoint جدید `/api/vehicles/custom/export?id=N`)
- دکمه‌ی "آپلود پروفایل": فایل JSON را می‌خواند، اعتبارسنجی ساختاری
  می‌کند (schema check ساده)، و به‌عنوان `status=UNVERIFIED` وارد
  می‌کند (حتی اگر در فایل verified بود — چون این دستگاه دیگر با آن
  فرمان‌ها روی این خودروی خاص تست نشده)

این قابلیت باعث می‌شود کاربر بتواند یک پروفایل را بین دستگاه‌ها/کاربران
دیگر (که همان مدل خودرو را دارند) به اشتراک بگذارد — ولی همیشه با
وضعیت اولیه‌ی احتیاطی.

---

## ۷. تغییرات لازم در فایل‌های موجود (فایل به فایل)

### ۷.۱ `vehicle_control.h` / `.cpp` — بازطراحی اصلی

**قبل:** CAN ID های هاردکد (`_canIdDoorLock = 0x1A0` و...) مستقیم در
سازنده.

**بعد:**
- یک متد جدید `bool executeCommand(const char* commandLabel)` که:
  1. از `ActiveProfileManager` (جدید) می‌پرسد "فرمان با این label
     برای خودروی فعلی چیست؟" (می‌تواند از DBC یا Learned/Manual بیاید)
  2. اگر پیدا نشد → false + لاگ
  3. اگر پیدا شد ولی `status != VERIFIED` (فقط برای منابع
     Learned/Manual؛ DBC چون از قبل عمومی تست‌شده به این چک نیاز
     ندارد) → false + پیام "این فرمان هنوز تأیید نشده"
  4. اگر آماده بود → همان مسیر `_sendCommand()` موجود (با همان
     rate-limit ۱۵۰ms که حفظ می‌شود) فراخوانی می‌شود
- توابع قدیمی (`lockAllDoors()`, `unlockAllDoors()`, ...) **حذف
  نمی‌شوند** (سازگاری با `main.cpp` موجود حفظ می‌شود) بلکه به‌صورت
  wrapper بازنویسی می‌شوند که داخلشان `executeCommand("lock_all")`
  و... را صدا می‌زنند. یعنی امضای عمومی کلاس دست‌نخورده می‌ماند.
- `setCustomCANIDs()` موجود برای سازگاری عقب‌رو نگه داشته می‌شود ولی
  دیگر مسیر اصلی نیست (مسیر اصلی از `ActiveProfileManager` است)

### ۷.۲ `vehicle_db.h` / `.cpp`

> **به‌روزرسانی بعد از پیاده‌سازی (v2.1):** برخلاف طرح اولیه‌ی این
> بخش («بدون تغییر ساختاری»)، در عمل `vehicle_db.h/.cpp` تغییر
> ساختاری هم گرفت: پشتیبانی از سیگنال‌های Motorola (`@0`) اضافه شد
> (فیلد `isBigEndian` در `DbcSignal` که قبلاً خوانده می‌شد ولی در
> استخراج/کدگذاری مقدار استفاده نمی‌شد، اکنون واقعاً در
> `extractSignalValue`/`encodeSignalValue` به کار می‌رود)، و لیست
> خودروهای وصل‌شده از ۳ به ۳۸ مدل گسترش یافت. جزئیات کامل در
> `CHANGELOG.md` بخش v2.1.

`VehicleDB` از نظر رابط عمومی (public API) بدون تغییر ماند —
`findWriteCommand` طبق طرح این بخش اضافه نشد چون در عمل مشخص شد
اکثر فایل‌های DBC موجود در این پروژه سیگنال نوشتن برای قفل/شیشه
ندارند (چون OpenDBC عمدتاً خواندنی است) — این یک واقعیت شناخته‌شده
است که در UI به کاربر گفته می‌شود (بخش ۹)، نه چیزی که با کد حل شود.

### ۷.۳ فایل‌های کاملاً جدید

| فایل | مسئولیت |
|---|---|
| `custom_vehicle.h` | ساختارهای `LearnedCommand`, `CustomVehicleProfile`, enum `CommandStatus{UNVERIFIED,VERIFIED}`, enum `CommandSource{SOURCE_DBC,SOURCE_LEARNED,SOURCE_MANUAL}` |
| `custom_vehicle_store.h/.cpp` | خواندن/نوشتن JSON در SPIFFS، لیست پروفایل‌ها، CRUD فرمان‌ها، export/import |
| `learn_engine.h/.cpp` | state machine بخش ۴: baseline capture، action capture، diff، candidate ranking. از `CANManager` موجود استفاده می‌کند (فقط با متدهای عمومی موجود `receiveMessageNonBlocking`/`flushRxQueue` — نیازی به تغییر `can_manager.cpp` نیست چون آن‌ها همین الان کفایت می‌کنند) |
| `active_profile_manager.h/.cpp` | لایه‌ی یکپارچه‌ساز: با توجه به این‌که کاربر کدام خودرو را فعال کرده (DBC یا Custom)، `executeCommand()` را به منبع درست هدایت می‌کند. بین `VehicleDB` و `CustomVehicleStore` میانجی است. |

### ۷.۴ `tft_ui.h` / `.cpp` — افزودن صفحات جدید (بدون حذف موجود)

سه تب موجود (کنترل/داشبورد/تنظیمات) دست‌نخورده می‌مانند. یک تب
چهارم اضافه می‌شود: **"یادگیری"** (یا زیرمنو در تنظیمات — تصمیم پیاده‌سازی
آزاد است، ولی باید از تب‌های موجود جدا باشد تا شلوغ نشود):

- صفحه‌ی wizard مطابق state machine بخش ۴.۱ (چند lv_obj که بر اساس
  `LearnModeState` نمایش/مخفی می‌شوند، شبیه الگوی `_passwordScreen`
  موجود در کد فعلی)
- صفحه‌ی فرم دستی (بخش ۵) — از همان کیبورد مجازی مشترک
  (`_keyboard` موجود) با یک ورودی hex استفاده می‌کند
- صفحه‌ی لیست پروفایل‌های سفارشی با وضعیت هر فرمان (✅/⚠️) و دکمه
  "تأیید و آماده‌سازی"

### ۷.۵ `webserver.h` / `.cpp` — endpoint های جدید

همه پشت همان `_authenticate()` موجود (Basic Auth) به‌علاوه همان
session token برای WebSocket:

```
GET  /api/vehicles/custom              لیست پروفایل‌های سفارشی
POST /api/vehicles/custom/new          ساخت پروفایل خالی جدید
POST /api/vehicles/custom/manual-add   افزودن فرمان دستی (بخش ۵)
GET  /api/vehicles/custom/export?id=N  دانلود JSON یک پروفایل
POST /api/vehicles/custom/import       آپلود JSON پروفایل

WebSocket message types جدید (روی همان /ws موجود، بعد از auth):
  {"type":"learn_start", "label":"lock_all"}
  {"type":"learn_capture_baseline"}
  {"type":"learn_wait_action"}
  {"type":"learn_capture_action"}
  {"type":"learn_candidates"}        (سرور → کلاینت: نتایج)
  {"type":"learn_select", "index":0} (کلاینت → سرور: انتخاب کاندید)
  {"type":"learn_save", "name":"..."}
  {"type":"verify_command", "vehicleId":0, "label":"lock_all"}
  {"type":"verify_confirm", "success":true}
```

این پیام‌ها از همان زیرساخت rate-limit و auth موجود در
`_handleWebSocketEvent` عبور می‌کنند (کد جدید در همان switch-case
اضافه می‌شود، نه یک مسیر موازی جدید).

### ۷.۶ `index.html` / `app.js` / `style.css` — بخش جدید در تب تنظیمات یا تب جدید

یک تب جدید "یادگیری" (مشابه ساختار تب‌های موجود در `index.html`)
با:
- دکمه شروع Learn Mode + نمایش state فعلی (baseline در حال ضبط...
  حالا دکمه رو بزن... در حال گرفتن...)
- لیست کاندیدها بعد از capture
- فرم دستی
- لیست پروفایل‌های من با دکمه‌های تأیید/حذف/export

الگوی موجود در `app.js` (توابع `handleMessage()` سوییچ روی `data.type`)
دقیقاً تکرار می‌شود، فقط case های جدید اضافه می‌شوند.

### ۷.۷ `config.h` — افزودن ثابت‌های جدید (بدون حذف موجود)

```cpp
#define MAX_CUSTOM_VEHICLES              8
#define MAX_LEARNED_COMMANDS_PER_VEHICLE 32
#define LEARN_BASELINE_MS              2000
#define LEARN_ACTION_CAPTURE_MS        2000
#define LEARN_ACTION_CAPTURE_MAX_MS    5000
#define BASELINE_MAX_IDS                 64
#define CANDIDATE_MAX                    10
```

### ۷.۸ `main.cpp` — سیم‌کشی نهایی

- ساخت instance های جدید: `CustomVehicleStore customVehicleStore;`,
  `LearnEngine learnEngine(canManager);`,
  `ActiveProfileManager activeProfileManager(vehicleDB, customVehicleStore);`
- `vehicleControl` به‌جای گرفتن CAN ID مستقیم، رفرنس
  `activeProfileManager` می‌گیرد
- در `handleCommand()` موجود، پیام‌های جدید (`learn_*`, `verify_*`)
  به متدهای مربوطه در `learnEngine`/`activeProfileManager` هدایت
  می‌شوند (case های جدید در همان دیسپچر موجود، ساختار if/else زنجیره‌ای
  فعلی حفظ می‌شود یا در صورت تمایل به enum-based dispatch ارتقا
  می‌یابد — تصمیم پیاده‌سازی)

---

## ۸. اصول ایمنی که باید در تمام کد جدید رعایت شوند (چک‌لیست الزامی)

این‌ها قوانین سخت‌گیرانه‌اند، نه پیشنهاد:

1. **Learn Mode هرگز فرمان نمی‌فرستد.** در تمام state های
   `LEARN_*`، فقط `receiveMessage*` صدا زده می‌شود، هرگز
   `sendMessage`.
2. **هیچ فرمان Learned/Manual بدون تأیید صریح کاربر اجرا نمی‌شود.**
   `status == VERIFIED` یک چک اجباری در `executeCommand()` است، نه
   فقط یک flag تزئینی در UI.
3. **دیالوگ تأیید باید CAN ID و بایت‌های واقعی را نشان دهد**، نه
   فقط "مطمئنید؟" — کاربر باید بداند دقیقاً چه چیزی فرستاده می‌شود.
4. **هیچ حلقه‌ی "امتحان تمام مقادیر ممکن" (brute force/fuzzing) در
   هیچ بخشی از کد نباید وجود داشته باشد.** این هم برای ایمنی فیزیکی
   خودرو و هم برای سازگاری با اصول Anthropic (کدی که می‌تواند به‌طور
   بالقوه مضر باشد) ممنوع است.
5. **همان rate-limit های موجود (۱۵۰ms در vehicle_control، ۳۰۰ms در
   وب) باید برای فرمان‌های Learned/Manual هم اعمال شوند** — یعنی از
   همان `_sendCommand()` مشترک عبور کنند، نه یک مسیر ارسال موازی.
6. **در UI (هم TFT هم وب) باید همیشه صادقانه گفته شود:**
   - "این فرمان تست‌نشده است" (برای UNVERIFIED)
   - "روی خودروهای دارای rolling code ممکن است این روش کار نکند"
     (یک بار در راهنمای Learn Mode، نه هر بار)
   - این‌ها متن‌های ثابت پیشنهادی هستند، دقیق‌شان در بخش ۹ آمده

---

## ۹. متن‌های پیشنهادی برای صداقت با کاربر (قابل استفاده مستقیم در UI)

```
[شروع Learn Mode]
"در این حالت، دستگاه فقط پیام‌های CAN Bus را می‌شنود و هیچ فرمانی
ارسال نمی‌کند. بعد از ضبط، باید خودتان کاندید درست را تأیید کنید."

[قبل از اولین اجرای یک فرمان یادگرفته‌شده]
"⚠️ این فرمان قبلاً تست نشده. با اجرای آن، پیام زیر مستقیماً روی
CAN Bus خودرو فرستاده می‌شود:
CAN ID: 0x123
Data: 01 00 00 00 00 00 00 00
این عمل ممکن است اثر فیزیکی فوری روی خودرو داشته باشد (مثلاً باز
شدن قفل). لطفاً مطمئن شوید خودرو در وضعیت امنی است."

[راهنمای کلی Learn Mode، یک‌بار نمایش داده شود]
"نکته مهم: بعضی خودروهای جدیدتر (معمولاً بعد از ۲۰۱۸ و برندهای
پریمیوم) از کدهای متغیر (rolling code) استفاده می‌کنند که با این
روش قابل یادگیری/تکرار نیستند. اگر بعد از تأیید، فرمان روی خودرو
کار نکرد، این می‌تواند دلیلش باشد."
```

---

## ۱۰. ترتیب توسعه (برای این نشست) — همه یک‌جا طبق خواسته‌ی کاربر

طبق تصمیم کاربر، تمام فایل‌ها **یک‌جا** (نه مرحله‌ای) تحویل داده
می‌شوند. ترتیب نوشتن داخلی (برای انسجام کد، نه برای تحویل جدا):

1. `custom_vehicle.h` (ساختارهای داده پایه)
2. `custom_vehicle_store.h/.cpp` (ذخیره‌سازی)
3. `learn_engine.h/.cpp` (منطق یادگیری)
4. `active_profile_manager.h/.cpp` (یکپارچه‌سازی)
5. `vehicle_control.h/.cpp` (بازنویسی با حفظ API قدیم)
6. `config.h` (افزودن ثابت‌ها)
7. `webserver.h/.cpp` (endpoint ها)
8. `tft_ui.h/.cpp` (صفحات جدید)
9. `main.cpp` (سیم‌کشی نهایی)
10. `index.html` / `app.js` / `style.css` (رابط وب)
11. به‌روزرسانی `README.md` و `CHANGELOG.md` با توضیح کامل این
    نسخه

همه‌ی فایل‌های بدون تغییر (`can_manager.*`, `obd2_reader.*`,
`vehicle_db.*` ساختار اصلی‌اش، `wifi_manager.*`) عیناً از نسخه‌ی
فعلی (که خودش قبلاً یک دور اصلاح باگ کامل خورده — رجوع کنید به
`CHANGELOG.md` موجود در ریشه‌ی پروژه) کپی می‌شوند.

---

## ۱۱. تست (چون کاربر گفته "کد باید در عمل واقعی بدون ایراد کار کند")

### ۱۱.۱ چیزی که از راه دور (توسط AI) قابل تضمین است
- کامپایل موفق با PlatformIO (`pio run`) بدون warning/error
- بررسی منطقی state machine (هیچ مسیر بن‌بست (dead-end) یا race condition واضح)
- رعایت تمام محدودیت‌های حافظه (اندازه‌گیری static با g++ مثل کاری
  که در CHANGELOG.md ثبت شده است)
- تست واحد منطق diff/candidate-ranking با داده‌ی CAN شبیه‌سازی‌شده
  (می‌تواند به‌صورت یک فایل تست جدا با g++ خالص، بدون سخت‌افزار،
  نوشته شود — پیشنهاد بخش ۱۲)

### ۱۱.۲ چیزی که فقط با سخت‌افزار واقعی توسط خود کاربر قابل تست است
- رفتار واقعی روی CAN Bus زنده‌ی یک خودروی مشخص
- صحت diff/candidate detection با نویز واقعی باس (که می‌تواند
  به‌مراتب شلوغ‌تر از هر شبیه‌سازی باشد)
- مسئله‌ی rolling code (فقط با تلاش واقعی replay قابل کشف است)
- کالیبراسیون تاچ‌اسکرین — **(به‌روزرسانی v2.1: دیگر placeholder
  نیست)** یک ویزارد کالیبراسیون واقعی ۵نقطه‌ای (`TFT_UI::runTouchCalibration()`،
  با استفاده از `TFT_eSPI::calibrateTouch()`) در اولین بوت اجرا و
  نتیجه در حافظه‌ی غیرفرار ذخیره می‌شود؛ آنچه هنوز فقط با سخت‌افزار
  واقعی قابل تأیید است، دقت واقعی این کالیبراسیون روی نمایشگر
  فیزیکی کاربر است، نه وجود خود مکانیزم

**این یعنی: من (یا هر AI) می‌توانم تضمین کنم کد از نظر منطقی/حافظه/
کامپایل درست است، ولی "کارکرد بدون ایراد روی خودروی واقعی" نهایتاً
فقط با تست کاربر روی سخت‌افزار واقعی قابل‌تأیید است.** این یک
محدودیت ذاتی کار با سخت‌افزار embedded است، نه کوتاهی در پیاده‌سازی.

---

## ۱۲. پیشنهاد برای ادامه (اختیاری، بعد از این نسخه)

> **به‌روزرسانی v2.1:** دو مورد از چهار پیشنهاد زیر (Motorola
> endianness و بازطراحی غیرمسدودکننده‌ی OBD2Reader) در نسخه‌ی v2.1
> پیاده‌سازی شدند — جزئیات در `CHANGELOG.md`. دو مورد باقی‌مانده
> (شبیه‌ساز CAN، HTTPS) هنوز پیاده نشده‌اند.

- افزودن یک "شبیه‌ساز CAN" (چند خط پایتون/C++ که روی PC پیام‌های
  فرضی تولید کند) تا کاربر بدون خودروی واقعی هم بتواند state machine
  Learn Mode را تست کند — **هنوز انجام نشده**
- ~~افزودن پشتیبانی Motorola endian در پارسر DBC~~ — **رفع شد در
  v2.1** (نگاشت بیت جداگانه برای `isBigEndian`، تست‌شده با شبیه‌سازی
  مستقل روی سیگنال‌های واقعی `bmw_e9x_e8x.dbc`)
- ~~بازطراحی غیرمسدودکننده‌ی `OBD2Reader::readAllPIDs`~~ — **رفع شد
  در v2.1** (state machine جدید در `OBD2Reader::update()`؛ توابع
  قدیمی مسدودکننده با برچسب `[BLOCKING]` برای سازگاری نگه داشته
  شدند)
- HTTPS با گواهی self-signed — **هنوز انجام نشده**

---

## ۱۳. لاگ خطا و تله‌متری (Error Logging / Telemetry) — v2.3

پیاده‌سازی ردیف ۱۶ چک‌لیست تجاری (`PROGRESS_CHECKLIST.md`). هدف:
وقتی کاربر می‌گوید «یهو قطع شد» یا «ری‌استارت شد»، نشست بعدی (انسان
یا AI) داده‌ی واقعی داشته باشد، نه فقط حافظه یا حدس.

فایل‌های جدید: `src/error_log.h`, `src/error_log.cpp` (کلاس
`ErrorLog`، singleton مشابه الگوی `getConfig()` در `config.h/.cpp`).

دو بخش، عمداً با دو مدل ماندگاری متفاوت:

- **حلقه‌ی رویدادها (RAM، با هر بوت پاک می‌شود):** آخرین ۴۰ رویداد
  (`ERRORLOG_MAX_ENTRIES`)، هرکدام با timestamp، دسته (`LogCategory`:
  SYSTEM/CAN/OBD2/WIFI/WEB/LEARN) و شدت (`LogSeverity`:
  INFO/WARN/ERROR). از طریق `GET /api/logs` (همان سطح auth بقیه‌ی
  API) و پیام وب‌سوکت `get_logs` قابل خواندن است.
- **شمارنده‌های تجمعی (NVS، ماندگار بین بوت‌ها):** تعداد کلی خطای
  ارسال/دریافت CAN، رویداد Bus-Off، شکست اتصال WiFi، تلاش ناموفق
  ورود وب، و خطای Learn Mode. برای جلوگیری از فرسایش فلش، حداکثر هر
  ۵ دقیقه یک‌بار ذخیره می‌شوند (`ErrorLog::maybeSaveCounters()`، صدا
  زده‌شده از `main.cpp`'s `loop()`)، نه در لحظه‌ی هر افزایش.

نقاط اتصال فعلی: `can_manager.cpp` (خطای ارسال/دریافت، Bus-Off)،
`wifi_manager.cpp` (شکست اتصال)، `webserver.cpp` (ورود/basic-auth
ناموفق)، `learn_engine.cpp` (تمام مسیرهای ورود به `LEARN_ERROR`)،
`main.cpp` (شکست بوت CAN).

**عمداً وصل نشده (نه فراموشی، تصمیم آگاهانه):** تایم‌اوت تک‌PID در
`OBD2Reader` — چون بخشی از عملکرد عادی است (خیلی از خودروها همه‌ی
PIDها را پشتیبانی نمی‌کنند)؛ ثبت هر تایم‌اوت به‌عنوان WARN، حلقه‌ی
رویدادها را در استفاده‌ی عادی به‌سرعت پر می‌کرد. اگر در آینده کسی
خواست این را اضافه کند، باید اول یک منطق de-duplication/rate-limit
برایش طراحی شود، نه یک `log()` ساده در همان نقطه.

**این ماژول هرگز `CANManager::sendMessage` را صدا نمی‌زند و مسیر
اجرای هیچ فرمانی را عوض نمی‌کند — فقط رویدادهایی که مسیرهای خطای
موجود از قبل تشخیص داده بودند را علاوه بر آن ثبت می‌کند.**

> **وضعیت تست:** مثل بقیه‌ی این پروژه، فقط از نظر منطقی/کامپایلی
> بررسی شده؛ رفتار واقعی NVS (نرخ ذخیره، بقای بین بوت‌های واقعی) روی
> سخت‌افزار واقعی تست نشده است.

---

## ۱۴. خلاصه‌ی یک‌خطی برای AI بعدی

> پروژه‌ی CarTouch (ESP32-S3 + CAN + LVGL + Web) یک نسخه‌ی v2 گرفته
> که در آن `vehicle_control` دیگر CAN ID هاردکد ندارد بلکه از سه
> منبع (DBC آماده، پروفایل یادگرفته‌شده از باس واقعی، ورودی دستی
> کاربر) از طریق `ActiveProfileManager` می‌خواند؛ هر فرمان جدید تا
> تأیید صریح کاربر (`VERIFIED`) در حالت `UNVERIFIED` قفل است و قابل
> اجرا نیست؛ Learn Mode هرگز چیزی به باس نمی‌فرستد (فقط شنود+diff)،
> و از v2.2 این تضمین در سطح درایور TWAI هم برقرار است، نه فقط سطح
> کد؛ از v2.3 یک ماژول لاگ/تله‌متری (`ErrorLog`، بخش ۱۳) رویدادهای
> خطای موجود در کل پروژه را هم ثبت می‌کند. تمام قابلیت‌های v1.0 (سه
> تب TFT، وب‌سوکت با session auth، DBC parser، OBD2، rate-limit،
> auto-sleep) دست‌نخورده حفظ شده‌اند. برای جزئیات کامل هر فایل، بخش
> ۷ این سند را دنبال کنید.
