# گزارش بررسی CarTouch — فایل‌های اصلاح‌شده و نکات باقی‌مانده

بر اساس بررسی کامل سورس واقعی (نه فقط README)، شامل تست عملی با g++ روی
تمام ۵۸ فایل DBC موجود در `data/dbc`.

## فایل‌های اصلاح‌شده (پیوست همین پیام)

### 1. `vehicle_db.h`
- **باگ بحرانی RAM**: `MAX_DBC_SIGNALS=200` باعث می‌شد آرایه‌ی داخلی
  `VehicleDB` حدود **۱٫۲۲ مگابایت** باشد (اندازه‌گیری‌شده با g++) —
  روی ESP32-S3 با ~512KB SRAM داخلی این به احتمال زیاد لینک/بوت را
  خراب می‌کند. کاهش یافت به ۲۴.

### 2. `vehicle_db.cpp`
- **باگ بحرانی پارسر**: فرمت `sscanf` خط `BO_` با فاصله‌ی اضافه قبل
  از `:` نوشته شده بود؛ چون فرمت واقعی هیچ فایل DBC (هر ۵۸ فایل چک
  شد) فاصله ندارد، **هیچ پیامی هرگز پارس نمی‌شد**. رفع شد و با
  `sscanf` واقعی تست شد (خروجی تست در پیام قبلی).
- **باگ سرریز CAN ID**: چند فایل واقعی (GM, Ford) از CAN ID گسترده
  بالای `INT_MAX` استفاده می‌کنند؛ `int`+`%d` این‌ها را منفی می‌کرد.
  به `uint32_t`+`%u` تغییر یافت.
- لیست خودروهای hardcode‌شده به فایل‌هایی اشاره می‌کرد که اصلاً در
  `data/dbc` وجود نداشتند (`toyota_camry.dbc`, `honda_civic.dbc`,
  `bmw_3series.dbc`, `generic_obd2.dbc`). به فایل‌های واقعاً موجود
  اصلاح شد (`toyota_prius_2010_pt.dbc`, `toyota_2017_ref_pt.dbc`,
  `bmw_e9x_e8x.dbc`) و "Generic/OBD-II" چون به DBC نیاز ندارد، بدون
  فایل تعریف شد.
- هشدار truncation اضافه شد وقتی فایل از سقف `MAX_DBC_MESSAGES` بیشتر
  است (قبلاً کاملاً بی‌صدا بود).

### 3. `webserver.cpp`
- **دور زدن احراز هویت**: `serveStatic("/", SPIFFS, "/")` هر فایل
  SPIFFS را (از جمله `index.html` با درخواست مستقیم) بدون
  `_authenticate()` سرو می‌کرد. جایگزین شد با روت‌های صریح فقط برای
  `app.js` و `style.css` (که حساس نیستند)؛ همه‌چیز دیگر پشت auth ماند.
- پرینت پسورد خام روی Serial در هر بوت حذف شد (فقط username چاپ
  می‌شود).

### 4. `config.h` و `README.md`
- کامنت/مستندات اشتباه "ILI9341" اصلاح شد به "ST7796" که واقعاً در
  `platformio.ini` تنظیم شده (`-DST7796_DRIVER=1`).

## مواردی که عمداً "اصلاح خودکار" نشدند (نیاز به تصمیم شما دارند)

این‌ها یا نیازمند دانش دقیق خودروی خودتان هستند یا تغییر معماری بزرگی
می‌خواهند که حدس‌زدنش روی یک خودروی واقعی خطرناک است:

1. **`vehicle_control.cpp` اصلاً به DBC/VehicleDB وصل نیست.** از CAN
   ID ثابت `0x1A0`-`0x1A5` با بایت‌های حدسی استفاده می‌کند. انتخاب
   خودرو از منو فعلاً هیچ اثری روی فرمان واقعی ندارد. برای وصل‌کردن
   واقعی، باید مشخص کنید کدام پیام/سیگنال دقیق DBC خودروی خودتان به
   قفل/شیشه/آینه مربوط است.
2. **پارسر DBC اندیان Motorola (`@0`) را پشتیبانی نمی‌کند** — فقط
   Intel/little-endian (`@1`) درست استخراج می‌شود؛ طبق کامنت خود کد
   "فعلاً هر دو یکسان پردازش می‌شوند" که برای سیگنال‌های Motorola غلط
   است.
3. **بدون HTTPS** — طبق `Help2.md` خودتان هم به‌عنوان کار باقی‌مانده
   ثبت شده بود.
4. **`data/dbc` شامل ۴٫۵ مگابایت داده است، ۱٫۱ مگابایتش پوشه‌ی
   `generator/`** (اسکریپت‌های پایتون OpenDBC که روی فرم‌ور کاملاً
   بی‌فایده‌اند و فقط فضای SPIFFS مصرف می‌کنند). توصیه: قبل از
   `pio run -t uploadfs` این پوشه را حذف کنید، و فایل‌های DBC را فقط
   به مدل‌هایی که واقعاً پشتیبانی می‌کنید محدود کنید (فایل‌هایی مثل
   `toyota_2017_ref_pt.dbc` با ۱۴۳ پیام و `bmw_e9x_e8x.dbc` با ۳۲۶
   پیام از سقف `MAX_DBC_MESSAGES=50` رد می‌شوند و truncate می‌شوند —
   جزئیات در کامنت‌های `vehicle_db.cpp`).
5. **`OBD2Reader::readAllPIDs` کاملاً blocking است** — در بدترین حالت
   (ECU جواب ندهد) تا ~۱٫۲ ثانیه در هر چرخه‌ی `loop()` می‌ماند که
   می‌تواند UI لمسی و WebSocket را کند/متوقف نشان دهد. نیاز به
   بازطراحی غیرمسدودکننده (state machine) دارد.
